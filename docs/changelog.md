# Nhật ký triển khai (Changelog)

> **Cách dùng tài liệu này:** lịch sử những gì đã CHẠY THẬT, theo mốc ngày/slice, mới nhất ở trên. Đây **không phải** nơi ghi việc sẽ làm (→ [docs/roadmap.md](./roadmap.md)) hay bài học/gotcha (→ [docs/lessons.md](./lessons.md)). Chi tiết kỹ thuật đầy đủ của mỗi phát hiện nằm ở addendum ADR tương ứng — mục dưới đây chỉ tóm tắt 1–2 dòng rồi trỏ sang, không thuật lại.
>
> **Cách cập nhật:** sau khi đóng một slice (thường đi kèm commit "Doc sync: đóng slice ..."), thêm 1 mục mới lên đầu danh sách dưới.

## 2026-09-14 — GUI ingest desktop: verify mã hoá `session.sqlite3` — ĐẠT

User xác nhận đã chạy `cargo tauri dev` + tài khoản thật: đăng nhập/mở lại app hoạt động đúng với `session.sqlite3` mã hoá (ADR-0021). Chưa có xác nhận riêng từng bước con (mở file bằng công cụ SQLite thường phải lỗi, nhánh di trú từ file plaintext cũ, key ổn định qua nhiều phiên `cargo tauri dev`). Checklist: [docs/pending-device-tests.md § mã hoá session.sqlite3](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--mã-hoá-sessionsqlite3-2026-09-14).

## 2026-09-14 — GUI ingest desktop: mã hoá `session.sqlite3` qua `Session` tự implement (ADR-0021)

Đóng gap `session.sqlite3` mà [ADR-0020](./adr/0020-ma-hoa-bi-mat-app-data-qua-os-keyring.md) cố ý để ngỏ — user chọn thẳng phương án "tự implement `Session`" sau brainstorm 3 phương án (EFS/không làm gì đều bị loại), yêu cầu ghim cứng version + quy trình kiểm tra lại khi upgrade. `ingest-grammers/src/encrypted_session.rs` (module mới) — `EncryptedSqliteSession` fork tay nguyên schema SQL + 8 method `Session` từ `grammers-session-0.10.0/src/storages/sqlite.rs`, chỉ khác một chỗ: mở kèm `EncryptionConfig` (libSQL encryption-at-rest, AES-256). Key sinh một lần bằng CSPRNG, lưu qua `secret_store::load_or_generate_key()` (tái dùng hạ tầng ADR-0020, không phát minh lại). `libsql = "=0.9.30"` ghim cứng đúng version `grammers-session` dùng nội bộ — nâng cấp bắt buộc đối chiếu lại `sqlite.rs` gốc trước khi merge (quy trình 4 bước, ADR-0021 mục 5).

Verify thật (không đụng MTProto): unit test roundtrip xác nhận file KHÔNG lộ header SQLite plaintext, mở sai key thì lỗi, mở đúng key đọc lại đúng dữ liệu — PASS. `cargo build`/`cargo clippy --workspace` (0 warning) sạch. Phát hiện phụ: bật feature `encryption` của `libsql` lần đầu cần `cmake` — máy dev có cả VS preview lẫn VS 2022 khiến `cmake` crate tự chọn generator sai tên, vá bằng `CMAKE_GENERATOR` (xem [docs/lessons.md](./lessons.md)). **Chưa verify** luồng đăng nhập đầy đủ qua `cargo tauri dev` + tài khoản thật. Chi tiết: [ADR-0021](./adr/0021-ma-hoa-session-sqlite-qua-session-tu-implement.md).

## 2026-09-14 — GUI ingest desktop: verify mã hoá app-data qua OS keyring — ĐẠT

User xác nhận đã chạy `cargo tauri dev` + tài khoản thật: đăng nhập/TMDB key hoạt động đúng qua OS keyring (ADR-0020). Chưa có xác nhận riêng từng bước con (nhánh di trú từ file cũ, xoá key, fallback plaintext...). Checklist: [docs/pending-device-tests.md § mã hoá app-data](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--mã-hoá-app-data-qua-os-keyring-2026-09-14).

## 2026-09-14 — GUI ingest desktop: mã hoá `credentials.json`/`tmdb_api_key.json` qua OS keyring (ADR-0020)

Đóng gap đã ghi từ SPIKE-10 (2026-09-05): hai file này lưu qua OS Credential Manager (crate `keyring`, module mới `secret_store.rs` — cổng DUY NHẤT gọi crate này), fallback về file plaintext y như trước nếu keyring không dùng được (yêu cầu user — không được "không lưu được gì" nếu thiếu backend OS). Tự di trú ngầm: máy có sẵn file cũ từ bản trước vẫn đọc được (rơi về fallback), lần ghi kế tiếp tự chuyển sang keyring và xoá file cũ. `commands.rs`/`tmdb.rs` đổi sang gọi qua `secret_store`, hành vi IPC bên ngoài không đổi.

Verify THẬT (an toàn để tự chạy — không đụng MTProto/tài khoản Telegram): unit test `#[ignore]` roundtrip qua Windows Credential Manager thật, `cargo test -- --ignored` PASS, đối chiếu `cmdkey /list` xác nhận dọn sạch sau test. `cargo build`/`cargo clippy --workspace` (0 warning) sạch. **Chưa verify** luồng đầy đủ qua `cargo tauri dev` + tài khoản thật. `session.sqlite3` (auth_key) NGOÀI phạm vi, vẫn chưa mã hoá. Chi tiết: [ADR-0020](./adr/0020-ma-hoa-bi-mat-app-data-qua-os-keyring.md).

## 2026-09-14 — GUI ingest desktop: verify màn Cài đặt — ĐẠT

User xác nhận đã chạy `cargo tauri dev` + tài khoản thật: màn Cài đặt hoạt động đúng thiết kế (xem thông tin tài khoản, quản lý TMDB key). Chưa có xác nhận riêng từng bước con (nhánh Esc dialog, tương tác với cảnh báo rời Workspace...). Checklist: [docs/pending-device-tests.md § màn Cài đặt](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-cài-đặt-2026-09-14).

## 2026-09-14 — GUI ingest desktop: màn Cài đặt (mới, ngoài mockup A.4 gốc) — quản lý TMDB key + xem thông tin tài khoản

Đóng gap ghi ở [docs/roadmap.md](./roadmap.md#ingest) ("Màn Settings — chưa có"). Route `/settings` mới (`ui/src/app/settings/`), vào qua icon ⚙ vừa wire ở topbar Workspace (đúng vị trí đã vẽ sẵn trong mockup A.3, `docs/ux-design.md`, trước đây chưa có gì đằng sau icon). Hai khối:

- **Tài khoản:** chỉ HIỂN THỊ (số điện thoại/API_ID/API_HASH che một phần bằng `maskSecret()`) đọc từ `credentials.json` qua `loadSavedCredentials()` đã có sẵn — không có nút đăng xuất/đổi số (`IngestRpc` hiện chưa có thao tác sign-out phía `grammers-client`, thêm nút mà không có RPC hỗ trợ sẽ để lại state nửa vời).
- **TMDB:** xem trạng thái (đã cấu hình/chưa), đổi key, xoá key — trước slice này cách DUY NHẤT "xoá key sai" là tự tay xoá file `tmdb_api_key.json` ở app-data (ghi rõ trong `describeTmdbError()`, ADR-0019). Dialog nhập/đổi key TÁI DÙNG NGUYÊN VẸN `TmdbKeyDialog`/`DialogService.promptTmdbApiKey()` — cùng dialog màn Workspace dùng lúc "Tra TMDB" lần đầu, không viết dialog thứ hai. Thêm command Rust mới `tmdb_delete_key` (`tmdb.rs`, xoá file best-effort).

**Cố ý KHÔNG làm ở slice này:** mã hoá `credentials.json`/`tmdb_api_key.json` (vẫn plaintext app-data) — màn này chỉ thêm nơi QUẢN LÝ, không đổi cách LƯU (mục riêng ở roadmap). `cargo build`/`cargo clippy --workspace` (0 warning) + `ng build`/`npm run lint` sạch — CHƯA verify bằng `cargo tauri dev` + tài khoản thật.

## 2026-09-14 — GUI ingest desktop: verify phân biệt lỗi TMDB "key sai" — ĐẠT

User xác nhận: lưu key sai → dialog tìm kiếm hiện đúng thông báo `InvalidKey` ("TMDB API Key không hợp lệ…"), không còn lẫn với lỗi mạng chung chung. Checklist: [docs/pending-device-tests.md § tích hợp tra cứu TMDB (PR3)](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--tích-hợp-tra-cứu-tmdb-pr3-2026-09-13).

## 2026-09-14 — GUI ingest desktop: phân biệt lỗi TMDB "key sai" (401) khỏi lỗi mạng khác

Đóng gap nhỏ ghi ở [ADR-0019](./adr/0019-tich-hop-tra-cuu-tmdb-o-buoc-draft.md): trước đó `fetch_tmdb()` (`tmdb.rs`) gộp chung MỌI lỗi HTTP qua `error_for_status()`, user không phân biệt được "gõ sai API key" với "mất mạng"/TMDB sập. Thêm `TmdbErrorDto::InvalidKey`, kiểm `response.status() == 401` TRƯỚC `error_for_status()` để tách riêng nhánh này — `Network` giờ chỉ còn lỗi request thật (không có response để đọc status) hoặc mã lỗi HTTP khác 401. `describeTmdbError()` (Angular) hiện thông báo tiếng Việt riêng cho `InvalidKey`, gợi ý xoá `tmdb_api_key.json` để nhập lại (chưa có màn Settings quản lý key, xem roadmap).

`cargo build`/`cargo clippy --workspace` + `ng build`/`npm run lint` sạch — CHƯA verify bằng key TMDB sai thật.

## 2026-09-14 — GUI ingest desktop: verify tier-aware upload threshold — ĐẠT cho Premium, hoãn nhánh thường

User xác nhận qua `cargo tauri dev` + tài khoản Premium thật: upload vẫn thành công đúng trần 4GB như hành vi cũ, không bị hạ nhầm xuống 2GB. **Nhánh tài khoản KHÔNG Premium chưa verify được — hiện không có tài khoản loại này để test** (hoãn, không phải lỗi) — logic đối xứng (`is_premium() == false` → `MAX_UPLOAD_BYTES_FREE`) đã có trong code nhưng chưa có bằng chứng thật. Checklist cập nhật: [docs/pending-device-tests.md § tier-aware upload threshold](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--tier-aware-upload-threshold-2026-09-14).

## 2026-09-14 — GUI ingest desktop: vá tier-aware upload threshold (Premium 4GB / thường 2GB, ADR-0017)

Đóng gap thật để ngỏ từ addendum ADR-0017 2026-09-13 (`MAX_UPLOAD_BYTES` hardcode 4GB chung cho mọi tài khoản, tài khoản không Premium vẫn có thể dính `FILE_PARTS_INVALID` thô với file 2-4GB). `ingest-grammers/src/rpc.rs` tách `MAX_UPLOAD_BYTES_PREMIUM`/`MAX_UPLOAD_BYTES_FREE`; `GrammersIngestRpc::new()` đổi thành `async fn`, gọi `client.get_me().await` một lần lúc đăng nhập xong để đọc cờ `premium` thật (`tl::enums::User::User(u).premium`), cache vào field `max_upload_bytes` trên struct. Lỗi đọc `get_me()` mặc định về trần thấp hơn (an toàn hơn). Ba call site dựng `GrammersIngestRpc` ở `src-tauri/src/commands.rs` (`check_session`/`submit_otp`/`submit_password`) đổi thành `.await`.

`cargo build`/`cargo clippy --workspace` (0 warning) sạch — CHƯA verify bằng tài khoản thật (cả hai nhánh Premium/không Premium). Chi tiết: [ADR-0017 § addendum 2026-09-14](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-14-vá-tier-aware-upload-threshold), checklist: [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--tier-aware-upload-threshold-2026-09-14).

## 2026-09-13 — GUI ingest desktop: đóng gói DLL FFmpeg vào bản phân phối — trả lời Đ2 (SPIKE-10), giữ được LGPL

Lần đầu `cargo tauri build` chạy trọn cho `apps/tsmc-ingest-desktop` (trước đó chỉ chạy `cargo tauri dev` nhờ vcpkg sẵn trên máy dev). Thêm `src-tauri/build.rs`: copy 7 DLL FFmpeg (danh sách khớp `tools/spike-09/README.md`) từ `$FFMPEG_DIR/bin` vào `ffmpeg-runtime/` (gitignored) **TRƯỚC KHI** gọi `tauri_build::build()` — phát hiện thật lúc code: gọi sau sẽ panic ngay trên build sạch vì `tauri_build::build()` tự validate glob `bundle.resources` NGAY LÚC BUILD. `tauri.conf.json::bundle.resources` map `ffmpeg-runtime/*.dll` → `""` để NSIS/MSI đặt DLL NGAY CẠNH `.exe` (đúng thứ tự Windows tìm DLL — thư mục exe trước `PATH`). Verify thật: `cargo tauri build` ra cả MSI (16.8MB) lẫn NSIS setup (12MB), 7 DLL xuất hiện đúng cạnh exe trong `target/release/`.

Nhân dịp đóng gói thật lần đầu, trả lời luôn câu hỏi Đ2 (SPIKE-10, để ngỏ từ 2026-09-07): grep configure string nhúng trong `avcodec-61.dll` ra `--disable-libx264 --disable-libx265`, và script mới `ingest-ffmpeg/examples/check_h264_encoder.rs` xác nhận encoder H264 THẬT ở `reencode.rs` (Hạng D) là `h264_mf` (Windows Media Foundation) — KHÔNG PHẢI `libx264` như comment cũ mô tả. **Kết luận: giữ LGPL, không kéo GPL.** Phát hiện phụ: option `preset=medium` (AVOption của libx264) gần như chắc chắn bị `h264_mf` bỏ qua thầm lặng — không phải bug (Hạng D đã verify chạy đúng thật), chỉ chạy bằng tham số mặc định thay vì preset developer định chọn. Sửa comment `reencode.rs` cho đúng sự thật, chưa đổi hành vi.

`cargo build`/`cargo clippy --workspace` (0 warning, gồm example mới) sạch. Chi tiết: [ADR-0013 § addendum 2026-09-13](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-13-trả-lời-đ2--đóng-gói-dll-ffmpeg-thật-lần-đầu), [docs/spikes/README.md § SPIKE-10](./spikes/README.md#cập-nhật-sau-khi-đóng-2026-09-13-đo-đ2-thật--lần-đầu-app-đóng-gói-exe).

## 2026-09-13 — GUI ingest desktop: verify TMDB (PR3) bằng API key thật — ĐẠT

User xác nhận đã verify bằng API key TMDB thật: nút "Tra TMDB" → dialog nhập key (lần đầu) → dialog tìm kiếm → chọn kết quả → Title/Năm cập nhật đúng. Xác nhận field response TMDB thật (`title`/`release_date`/`poster_path`...) khớp giả định ghi trong [ADR-0019](./adr/0019-tich-hop-tra-cuu-tmdb-o-buoc-draft.md) — không cần sửa `tmdb.rs`. Chưa có chi tiết từng bước riêng (nhánh `kind: 'episode'`/`search/tv`, đóng dialog không chọn gì). Checklist: [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--tích-hợp-tra-cứu-tmdb-pr3-2026-09-13).

## 2026-09-13 — GUI ingest desktop: tích hợp tra cứu TMDB ở bước Draft (PR3, ADR-0019)

Đóng gap "để ngỏ" từ ADR-0017/ux-design.md — tra metadata online, opt-in mặc định tắt. Thiết kế đầy đủ + phương án đã loại ở [ADR-0019](./adr/0019-tich-hop-tra-cuu-tmdb-o-buoc-draft.md), tóm tắt code thật:

- **Rust (`tmdb.rs` mới, không thuộc `IngestRpc`):** `tmdb_has_key`/`tmdb_save_key` (key TMDB v3 lưu app-data `tmdb_api_key.json`, CÙNG mô hình `credentials.json` — không phải `localStorage`) + `tmdb_search(query, kind)` dùng `reqwest` mới thêm vào `Cargo.toml`, gọi `search/movie` hoặc `search/tv` tuỳ `kind`, gộp kết quả về một shape chung (`TmdbSearchResultDto`: id/title/year/poster_url).
- **Angular:** nút "Tra TMDB" (icon kính lúp) mỗi dòng bảng metadata (Draft) — chưa có key thì mở `TmdbKeyDialog` trước (opt-in tự nhiên, không cần màn Settings); có key thì mở `TmdbSearchDialog` (query mặc định = title đã seed, admin sửa lại được, danh sách kết quả có poster nhỏ để TỰ CHỌN — không tự động điền). Chọn xong chỉ ghi `title`+`year`, `metaSource: 'manual'` (không thêm `'tmdb'` vào enum — tránh đổi Catalog Spec ADR-0010).
- Bảng metadata đổi từ 8 sang 9 cột (`workspace.scss::$columns`) để chứa nút mới.

`cargo build`/`cargo clippy --workspace` (0 warning) + `ng build` (322.16KB, không đổi budget) sạch. **CHƯA verify bằng API key TMDB thật** — field name (`title`/`name`/`release_date`/`first_air_date`/`poster_path`) dựa trên tài liệu TMDB v3 công khai, không phải đã đo (ADR-0019 § "Giới hạn thật"). Cần admin tự có key thật để test.

## 2026-09-13 — GUI ingest desktop: verify Đ1 (SPIKE-10) — ĐẠT, cài+chạy bản đóng gói trên máy sạch

User xác nhận cài đặt bản `.msi`/NSIS setup (kèm 7 DLL FFmpeg qua `build.rs` mới) trên một máy KHÔNG có `vcpkg`/LLVM/Rust/Node → mở app chạy đúng, không còn lỗi "thiếu library cần thiết của ffmpeg" đã gặp trước bản vá ở mục trên. Mức bằng chứng: "cài + mở chạy được" — chưa có số liệu chi tiết (tổng MB installer, danh sách file runtime đối chiếu từng cái, thử riêng Hạng D trên máy đó). Đ1+Đ2 (SPIKE-10) nay đều ĐẠT — spike vẫn giữ nguyên trạng thái đóng 🟡 (lý do gốc là M6 `FLOOD_WAIT` để ngỏ có chủ đích, không đổi). Checklist còn mở: [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--đóng-gói-bản-phân-phối-exeinstaller-trên-máy-sạch-2026-09-13).

## 2026-09-13 — GUI ingest desktop: vá bug KHÔNG ĐÓNG ĐƯỢC APP — thiếu quyền `core:window:allow-destroy` (regression từ PR2)

User báo lỗi console `"window.destroy not allowed. Permissions associated with this command: core:window:allow-destroy"` và không tắt được app sau khi thêm guard `onCloseRequested` ở PR2 (mục dưới). Nguyên nhân đọc thẳng `node_modules/@tauri-apps/api/window.js`: wrapper `onCloseRequested()` tự gọi `await this.destroy()` SAU KHI handler chạy xong nếu handler không `preventDefault()` — nghĩa là chỉ CẦN đăng ký listener này, MỌI lần đóng (kể cả nhánh không có draft/queue nào) đều cần quyền `core:window:allow-destroy`, không phải chỉ nhánh có cảnh báo. `capabilities/default.json` trước đó chỉ có `core:default` (không gồm quyền window). Đây là bug do tôi (assistant) gây ra ở PR2 — chưa test thật trường hợp "không có gì đang chạy" trước khi báo hoàn tất.

Vá: thêm `"core:window:allow-destroy"` vào `capabilities/default.json`; đổi `app.ts::guardWindowClose()` từ gọi `appWindow.close()` sang `appWindow.destroy()` khi user xác nhận đóng (doc `@tauri-apps/api/window`: `close()` chỉ EMIT LẠI chính sự kiện `closeRequested`, `destroy()` mới thật sự đóng và không lặp lại sự kiện — bỏ luôn cờ `closeConfirmed` chống lặp vô hạn vì không còn cần).

`cargo build --workspace` sạch (regenerate ACL bindings từ capability mới) + `ng build` sạch, budget không đổi (321.72KB) — **CHƯA verify lại bằng app thật** (cả nhánh đóng bình thường lẫn nhánh có cảnh báo). Checklist: [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-13 — GUI ingest desktop: chặn kích thước file TRƯỚC khi upload — đóng gap thật của SPIKE-10 M7 (ADR-0017)

Verify thật: thả `tools/spike-10/sample-4gb.mp4` (4 645 817 639 byte — đúng file M7 đã dùng đo ngưỡng) vào Workspace → upload thất bại với lỗi giao thức thô `FILE_PARTS_INVALID` (RPC 400) thay vì thông báo dễ hiểu. **Không phải bug mới** — SPIKE-10 M7 đã đo đúng ngưỡng này và khuyến nghị tường minh "chặn TRƯỚC khi mở kết nối, không phụ thuộc server phản hồi nhanh/chậm" (`tools/spike-10/README.md`), nhưng khuyến nghị đó chưa từng được port vào `ingest-grammers` thật khi scaffold — `IngestRpcError::FileTooLarge` tồn tại sẵn trong trait nhưng không nơi nào từng ném ra nó. 5 file còn lại trong đợt test (dưới trần) upload thành công; vùng bảng metadata verify đúng.

Vá: hằng số `MAX_UPLOAD_BYTES = 4_000_000_000` (`ingest-grammers/src/rpc.rs`), chặn ngay đầu `upload_video()` bằng `total` đã có sẵn (`tokio::fs::metadata`), trả `FileTooLarge { max_bytes, actual_bytes }` thay vì mở kết nối. `describeIngestError()` (Angular) đổi sang hiện số GB cụ thể cả hai chiều thay vì câu chung chung.

`cargo build`/`cargo clippy --workspace` (0 warning) + `ng build` (321.72KB, không đổi budget) sạch — **CHƯA verify lại bằng tài khoản thật sau bản vá**. Giới hạn đã biết: ngưỡng `4_000_000_000` byte là con số công khai Premium, chưa bisect chính xác byte; **chưa kiểm chứng ngưỡng tài khoản không Premium** (có thể thấp hơn). Chi tiết: [ADR-0017 § addendum 2026-09-13](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-13-đóng-gap-thật-của-m7--chặn-kích-thước-file-trước-khi-upload), checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-13 — GUI ingest desktop: hydration + singleton store + guard rời màn + dialog Hạng D gộp (PR2, ADR-0018)

Hoàn thiện phần còn lại của [ADR-0018](./adr/0018-task-id-lam-khoa-tuong-quan-ipc-ingest-desktop.md) sau bản vá Task ID (PR1, mục dưới):

- **Hydration** (`get_current_task()`/`clear_current_task()`, Rust): `AppState.current_task` (snapshot 1-slot, `std::sync::Mutex` — lý do chọn thay `tokio::sync::Mutex` xem addendum ADR) cập nhật ở mọi điểm emit `"upload-progress"`/`"pipeline-stage"`. `workspace.ts` gọi SAU KHI đăng ký xong hai listener, vá khoảng hở "listener bị gỡ lúc component unmount, event phát ra trong lúc đó bị lọt mất".
- **`DraftStore`/`QueueStore`** (`core/draft-store.ts`, `core/queue-store.ts`, `providedIn: 'root'`): tách state (không phải logic) khỏi `WorkspaceComponent` — phát hiện lúc code: `startUpload()` là async/await JS thuần, router huỷ component KHÔNG huỷ theo Promise chain đang chạy dở, nên cần state sống ngoài vòng đời component để remount thấy đúng tiến trình thật thay vì rỗng. `workspace.ts` alias biến cũ thẳng vào signal store, không đổi logic nghiệp vụ.
- **`canDeactivateWorkspace`** (`workspace-deactivate.guard.ts`) + **`onCloseRequested`** (`app.ts`): cảnh báo mất Draft/tác vụ đang chạy trước khi rời `/workspace` hoặc đóng cửa sổ Tauri trực tiếp — xác nhận thì xoá Draft, giữ nguyên Queue (tiến trình Rust chạy ngầm, không quan tâm route Angular).
- **Dialog Hạng D gộp** (`shared/dialog/grade-d-dialog.ts`): một dialog cho cả batch, toggle riêng từng file (mặc định checked) — thay hỏi từng file một dialog riêng.
- **Vá bug ngân sách bundle thật:** `inject(DialogService)` tĩnh ở `App`/guard (cả hai đều eager, không qua `loadComponent`) kéo `MatDialogModule` vào bundle chính, `ng build` báo vượt 500KB (567KB) — vá bằng `import()` động + `runInInjectionContext`, về lại 321.72KB.

`cargo build`/`cargo clippy --workspace` (0 warning) + `ng build` sạch, dưới budget — **CHƯA verify bằng tài khoản Telegram thật**. Checklist: [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12). Chi tiết đầy đủ: [ADR-0018 § Cập nhật 2026-09-13, PR2](./adr/0018-task-id-lam-khoa-tuong-quan-ipc-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-13-pr2--hydration--singleton-store--guard--dialog-hạng-d-code-thật).

## 2026-09-13 — GUI ingest desktop: verify thật bản vá Task ID — ĐẠT

User xác nhận đã chạy `cargo tauri dev` + tài khoản Telegram thật sau bản vá Task ID ở mục dưới: progress upload chuyển đúng sang xác định (%) với Hạng B/C/D, không còn đứng mãi ở `indeterminate`. Chưa có chi tiết từng bước riêng (hạng cụ thể nào, có quan sát race huỷ-nhầm-file lúc giao ca giữa 2 file hay không) — checklist tương ứng cập nhật ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-13 — GUI ingest desktop: vá bug progress "indeterminate" — Task ID (UUID) thay path làm khoá tương quan IPC, ADR-0018

Vá open issue ghi ở mục dưới (2026-09-12), theo [ADR-0018](./adr/0018-task-id-lam-khoa-tuong-quan-ipc-ingest-desktop.md): UUID (`crypto.randomUUID()`) sinh phía Angular ngay lúc đẩy item từ bảng metadata sang hàng đợi (`startUpload()`), gắn vào `UploadQueueItem.taskId`, truyền xuống MỌI command/event liên quan tới item đó thay vì `path`:

- `upload_video`/`prepare_upload`/`cancel_upload` (Tauri command) nhận thêm tham số `task_id`; `UploadProgressDto`/`PipelineStageDto` thêm field `task_id` làm khoá tương quan thật — `path` giữ lại CHỈ để hiển thị/log.
- `AppState.active_cancel` đổi từ `Mutex<Option<CancelFlag>>` sang `Mutex<Option<(String, CancelFlag)>>` — `cancel_upload(task_id)` giờ **idempotent theo id**: no-op nếu `task_id` không khớp task đang giữ cờ, vá luôn một race có thật (lệnh huỷ trễ tới đúng lúc giao ca sang file kế tiếp trong queue có thể huỷ NHẦM file đó ở bản cũ không có id).
- `workspace.ts`: mọi chỗ so khớp `item.path` để cập nhật `uploadQueue` đổi sang so khớp `item.taskId` (`updateQueueItem`, `dismissQueueItem`, nút Huỷ, `trackByTaskId` mới cho `*cdkVirtualFor` của hàng đợi). Bảng metadata (`queue`, Draft) không đổi — vẫn dùng `path`, không cần `taskId` vì không bao giờ nhận event Rust.

Phạm vi CỐ Ý thu hẹp so với thảo luận thiết kế ban đầu (đã đọc code trước khi viết ADR, phát hiện giả định sai): pipeline hiện chạy TUẦN TỰ thật (`AppState.active_cancel` chỉ có 1 slot, không phải registry đa tác vụ) — quyết định giữ đúng mô hình "một current-task", KHÔNG dựng `DashMap`/danh sách tác vụ đang chạy. Hydration khi remount (`get_current_task()`), singleton `QueueStore`/`DraftStore`, `CanDeactivate` guard, và dialog Hạng D dạng toggle từng file (ghi ở ADR-0018 § "Quyết định kèm theo") **CHƯA làm ở slice này** — để dành PR kế tiếp (hand-off UI dựa trên `taskId`).

`cargo build`/`cargo clippy --workspace` (0 warning) + `ng build` (`apps/tsmc-ingest-desktop/ui`) sạch — **CHƯA verify bằng `cargo tauri dev` + tài khoản Telegram thật** (vùng code này đã verify thật cho Hạng C/D trước khi đổi IPC — đổi vào đây là rủi ro hồi quy, cần admin tự chạy lại, CLAUDE.md: không chạy đăng nhập MTProto hộ người dùng). Checklist cập nhật ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: wrap phiên — phát hiện, CHƯA vá, open issue "progress upload luôn indeterminate"

User báo tiếp: sau bản vá spinner ở mục dưới, spinner lúc "Đang upload video…" xoay đúng, NHƯNG luôn ở chế độ `indeterminate` (vòng xoay vô định) — KHÔNG BAO GIỜ chuyển sang `determinate` để hiện % byte thật, dù `queueItemProgressMode()` (đã sửa ở mục dưới) đúng lý sẽ chuyển ngay khi có mẫu tiến trình đầu tiên.

**Nguyên nhân gốc xác định được (đọc code, CHƯA vá):** lệch **correlation id** giữa backend và frontend — đúng loại lỗi CLAUDE.md cảnh báo ("mọi message xuyên luồng phải có correlation id", nhưng ở đây hai đầu dùng HAI giá trị khác nhau cho cùng một field tưởng là chung id.

- `upload.rs::upload_video()` bắn sự kiện `"upload-progress"` với `path: file_path.clone()` — `file_path` chính là tham số IPC nhận từ frontend.
- `workspace.ts::processItem()` gọi `uploadVideo({ filePath: prepared.remuxed_path, ... })` (dòng ~662) — `prepared.remuxed_path` là đường dẫn file TẠM sau remux/re-encode (nằm trong `temp_dir`, do `prepareUpload()` sinh ra), KHÁC HẲN `item.path` (đường dẫn file gốc trên đĩa của user).
- `onUploadProgress()` handler (dòng ~269) nhận sự kiện, gọi `this.updateQueueItem(p.path, {...})` — `updateQueueItem()` tìm item trong `uploadQueue` khớp theo `item.path` (đường dẫn GỐC). Vì `p.path` (từ backend) = đường dẫn TẠM còn khoá tìm kiếm = đường dẫn GỐC → KHÔNG BAO GIỜ khớp → patch bị rơi im lặng (`updateQueueItem` không tìm thấy phần tử nào để cập nhật, không log lỗi vì đây không phải path sai, chỉ là hai bên "cùng gọi là path nhưng khác giá trị") → `item.progress` không bao giờ được set → `queueItemProgressMode()` luôn thấy `!item.progress` → mãi mãi `indeterminate`.

**Vì sao slice trước không phát hiện ra bằng review code:** logic `queueItemProgressMode()`/sự kiện `onUploadProgress()` đều ĐÚNG riêng lẻ khi đọc từng file một — bug chỉ lộ ra khi đối chiếu CHÉO giá trị thực tế của `file_path` ở hai đầu gọi `uploadVideo()`, thứ chỉ user chạy `cargo tauri dev` thật với file cần remux/re-encode (Hạng B/C/D — Hạng A KHÔNG qua remux nên `remuxed_path` tình cờ CÓ THỂ trùng `item.path` nếu chỉ đổi đuôi tại chỗ, cần xác nhận riêng có phải đây là lý do Hạng A từng "có vẻ" chạy đúng % hay không) mới lộ ra.

**CHƯA vá trong slice này — để lại làm việc đầu tiên của slice sau.** Hướng vá dự kiến (chưa code, chỉ ghi lại để không mất ngữ cảnh): đổi correlation id ở `on_progress`/sự kiện `"upload-progress"` từ `file_path` (đường dẫn tạm) sang giá trị GỐC ổn định xuyên suốt một item (vd truyền thêm `original_path` riêng vào `upload_video` command, hoặc đơn giản hơn — đổi `updateQueueItem()`/`onUploadProgress()` phía frontend tự nhớ "path tạm đang active" thay vì so khớp lại `item.path`). Cần cân nhắc kỹ hướng nào rồi mới sửa, không vá vội giữa lúc wrap phiên.

`cargo build`/`ng build`/`npm run docs:check` không đổi (chỉ đọc code + cập nhật tài liệu, KHÔNG sửa code trong slice này) — xem checklist mở tương ứng ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: vá 4 bug thật từ phiên test tiếp theo (spinner đứng yên, cuộn ngang, upload không thấy spinner, re-encode lỗi "invalid argument")

User tiếp tục test bằng `cargo tauri dev` + tài khoản thật, báo 4 vấn đề cùng lúc — cả 4 đều có NGUYÊN NHÂN GỐC xác định được, không phải "thử lại xem sao".

**1. Spinner hàng đợi đứng yên (không xoay) lúc re-encode/remux — nguyên nhân gốc: `provideNoopAnimations()`.** App dùng noop animations từ slice màn Đăng nhập (lý do lúc đó: "công cụ desktop tối thiểu, chưa cần transition thật") — hoá ra `mat-progress-spinner` không coi animation của nó là một "hiệu ứng trang trí" bỏ qua được: Material tự thêm class `_mat-animation-noopable` khi phát hiện noop animations, ép `animation: none !important` lên chính vòng xoay indeterminate (đọc trực tiếp từ CSS biên dịch sẵn của `@angular/material`, không đoán) — spinner render ra một VÒNG TĨNH thay vì xoay. Sửa: đổi `provideNoopAnimations()` → `provideAnimationsAsync()` (`@angular/platform-browser/animations/async`) trong `app.config.ts` — tải animation engine LƯỜI (chunk `browser` riêng ~68kB, xác nhận qua `ng build`: `main.js` giảm từ 337kB xuống 284kB), không phải `provideAnimations()` (eager) vì không cần animation ngay từ frame đầu.

**2. Sidebar hàng đợi cuộn NGANG, tên file bị cắt cụt thay vì hiện "…".** `cdk-virtual-scroll-viewport` tự đặt `overflow: auto` CẢ HAI trục — thiếu `overflow-x: hidden` tường minh nên trình duyệt hiện thanh cuộn ngang thay vì ép nội dung co lại theo bề rộng khung (Material tự có `overflow:hidden` + `flex:1` trên `.mdc-list-item__content` nên LẼ RA co đúng, nhưng viewport cho phép cuộn ngang che mất hiệu ứng đó). Vá bằng `overflow-x: hidden` trên `.queue-list` + `min-width:0`/`max-width:100%` trên `.queue-row`/`.file-name`/`.stage-line` (phòng thủ, không dựa hết vào CSS nội bộ của Material).

**3. Lúc "Đang upload video…" không thấy spinner nào (khác lúc remux/re-encode, ít nhất VẪN hiện vòng tĩnh).** Nguyên nhân: `queueItemProgressMode()` chuyển `"determinate"` NGAY khi bước vào stage `uploading_video`, TRƯỚC KHI sự kiện `"upload-progress"` đầu tiên kịp tới — một vòng determinate ở value=0% là một vòng TRỐNG HOÀN TOÀN theo đúng thiết kế MDC (stroke-dashoffset ở 0% = không có gì để vẽ), dễ hiểu nhầm là "spinner không hiện". Sửa: chỉ chuyển "determinate" sau khi ĐÃ có ít nhất một mẫu tiến trình thật (`item.progress` tồn tại VÀ `totalBytes > 0`) — trước đó giữ "indeterminate" (vẫn xoay, thấy ngay có việc đang chạy dù chưa có % cụ thể).

**4. `reencode_to_mp4()` (Hạng D) lỗi "invalid argument" trên một file thật — ĐÚNG như rủi ro đã ghi nhận trước ở "Nếu có gì vỡ" của `docs/pending-device-tests.md`.** Nguyên nhân xác nhận qua fixture tái hiện được (không phải đoán): bản đầu gán THẲNG `encoder.set_format(decoder.format())` + kích thước gốc — `avcodec_open2` của `libx264` từ chối khi (a) pixel format nguồn không phải 4:2:0 phẳng chuẩn (test tái hiện bằng fixture MJPEG/AVI cố ý `yuvj444p`), (b) chiều rộng/cao LẺ (test tái hiện bằng fixture 641×361 cố ý — 4:2:0 cần chẵn cả hai chiều). CLI shell-out cũ không gặp lỗi này vì binary `ffmpeg` tự chèn ngầm bước `scale`/`format` khi cần; native API (`ffmpeg-next`) KHÔNG tự làm hộ. Vá bằng MỘT `scaling::Context` BẮT BUỘC (luôn chạy, không điều kiện) ép mọi frame về `Pixel::YUV420P` + kích thước làm tròn XUỐNG số chẵn gần nhất, TRƯỚC khi đưa vào encoder — cùng pattern đã có sẵn ở `thumbnail.rs`. Verify lại bằng CHÍNH fixture tái hiện được lỗi (641×361, `yuvj444p`) — chạy sạch, output đúng 640×360/H.264/AAC, `ffmpeg -f null -` decode lại không lỗi; regression-check lại fixture Xvid gốc (đã verify slice trước) vẫn chạy đúng, không hồi quy.

`cargo build`/`cargo clippy` (0 warning) + `ng build`/`ng serve`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch. **CHƯA verify cả 4 fix bằng `cargo tauri dev` thật** (chỉ verify riêng #4 bằng fixture tổng hợp tái hiện đúng lỗi gốc, #1-3 chỉ verify qua build/lint) — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: hàng đợi đổi sang `mat-list`/`mat-progress-spinner` + vá lệch icon-button

Theo yêu cầu user, hai việc trên CÙNG vùng hàng đợi (sidebar trái) vừa redesign ở mục dưới:

**Vá bug lệch ripple/icon của `.row-action-button`:** bản trước ép cứng `width`/`height`/`line-height: 26px` cho `mat-icon-button` — padding NỘI BỘ của Material (M3 theming) tính bằng `calc((state-layer-size - icon-size) / 2)` dựa trên HAI custom property (`--mat-icon-button-state-layer-size`/`--mat-icon-button-icon-size`), không đọc `width`/`height` CSS thường — ép khung ngoài nhỏ lại mà không đổi hai biến đó làm ripple (vẽ theo state-layer-size mặc định 40px) tràn ra ngoài khung 26px đã bị bóp, icon cũng lệch tâm theo. Sửa đúng gốc: bỏ `width`/`height`/`line-height`, đặt `--mat-icon-button-state-layer-size: 28px` + `--mat-icon-button-icon-size: 16px` — Material tự tính lại padding khớp khung mới, ripple/icon về đúng tâm.

**Hàng đợi đổi từ div tự vẽ sang `mat-list`/`mat-list-item` thật:** mỗi dòng giờ dùng `matListItemAvatar` (icon trạng thái — dấu tick khi "Xong", dấu X khi "Lỗi", đồng hồ khi "Trong hàng đợi…", KHÔNG còn chữ text như bản trước), `matListItemTitle` (tên file, ellipsis, `matTooltip` hiện tên đầy đủ), `matListItemLine` (MỘT dòng phụ hiện đúng tên stage — "Đang remux…"/"Đang re-encode…"/"Đang upload video…", không kèm số % nữa), `matListItemMeta` (nút Huỷ/dismiss, y hệt hành vi cũ). `itemSize` của `cdk-virtual-scroll-viewport` đổi 56px → 64px, khớp đúng `--mat-list-list-item-two-line-container-height` mặc định của MDC (đọc từ CSS biên dịch sẵn của `@angular/material`, không đoán).

**`mat-progress-bar` (thanh ngang) → `mat-progress-spinner` (vòng tròn), tận dụng LUÔN avatar làm chỗ hiện số:** vòng ngoài là spinner (`mode="determinate"` có % thật CHỈ ở bước `uploading_video`, `"indeterminate"` mọi bước cục bộ khác VÀ lúc đang chờ `FLOOD_WAIT`), số ở GIỮA vòng (`spinnerCenterText()` — ưu tiên hiện giây `FLOOD_WAIT` còn lại nếu có, kế đến % byte, không có gì nếu đang ở bước không đo được %) đặt overlay tuyệt đối đúng tâm ô 32×32px.

`ng build`/`ng serve` (đã re-verify chunk chứa `MatList`/`MatProgressSpinner`/`spinnerCenterText`)/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch. `cargo build`/`cargo clippy` không đổi (round này thuần Angular/CSS). **CHƯA verify bằng `cargo tauri dev` thật** — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: verify thật Hạng D + tách hàng đợi khỏi bảng metadata + shared dialog

**Verify thật (user tự chạy `cargo tauri dev` + tài khoản Telegram thật):** "Hạng D re-encode và upload thành công" — xác nhận `reencode_to_mp4()` (mục dưới, mới verify bằng fixture tổng hợp trước đó) chạy đúng trên tài khoản thật VÀ file Hạng D thật của admin, không chỉ trên fixture AVI/Xvid sinh tay. Chi tiết từng bước (đúng nội dung dialog xác nhận, đúng nhãn stage, đúng hình ảnh/âm thanh sau re-encode) CHƯA được xác nhận riêng lẻ — checklist giữ các dòng con ở trạng thái mở.

**Redesign UI theo yêu cầu user — tách BẢNG METADATA khỏi HÀNG ĐỢI (trước đó hai vùng lặp lại CÙNG một danh sách):**

- Bảng metadata (cột phải, `queue` signal) giờ CHỈ chứa file CHƯA bắt đầu upload — nơi duy nhất sửa Title/Season/Ep/Năm/chọn dòng. Sidebar trái (`uploadQueue` signal MỚI, kiểu "Queued files" của FileZilla / trạng thái gửi mail Apple Mail) RỖNG cho tới khi bấm "Upload" — lúc đó các dòng đã chọn bị XOÁ khỏi bảng metadata và ĐẨY sang hàng đợi (kèm snapshot `rank`/`metadata`/`durationSec` cần cho `processItem()`, vì bảng gốc không còn giữ dòng đó nữa), xử lý xong vẫn ở lại hàng đợi (trạng thái "Xong"/"Lỗi") cho tới khi người dùng bấm nút "×" dismiss riêng (không tự động biến mất).
- Mỗi dòng hàng đợi thêm `mat-progress-bar` MỎNG ngay dưới tên file — `mode="determinate"` (có % thật) chỉ ở stage `uploading_video` (RPC MTProto DUY NHẤT báo tiến trình byte), `mode="indeterminate"` ở mọi stage cục bộ khác (remux/re-encode/thumbnail/rút phụ đề/upload phụ đề), ẩn hẳn ở `queued`/`done`/`error`.
- Global progress kiểu Apple Mail ("góc dưới sidebar"): thêm `batchTotal`/`batchDone` signal, hiện "Đã tải X/N file — P%" dưới hàng đợi trong lúc batch chạy — đếm CẢ file bị bỏ qua (từ chối re-encode Hạng D) là "đã xử lý", không chỉ file thành công.
- Nút "Upload (N)" xác nhận ĐÃ bind đúng `uploadableItems().length` (đếm theo checkbox đã tick + đã probe xong, KHÔNG loại Hạng D) — hành vi này thật ra đã đúng từ lần mở khoá Hạng D (mục dưới), không phải bug mới; ghi lại vì user nêu lại yêu cầu này.
- Bảng metadata thêm cột nút "×" cuối mỗi dòng (xoá nhanh MỘT dòng, tương đương chọn 1 checkbox + "Xoá khỏi hàng" nhưng ít thao tác hơn) — bù lại nút xoá-khỏi-hàng-đợi cũ ở sidebar trái, nay không còn ý nghĩa vì sidebar không còn hiện file CHƯA upload.

**Shared confirm/alert dialog (theo yêu cầu user — thay `window.confirm()` bằng Angular Material thật):** `shared/dialog/confirm-dialog.ts` (component thuần đọc `MAT_DIALOG_DATA`, `mode: 'alert' | 'confirm'` quyết định có nút Huỷ hay không, `tone: 'warn'` tô đỏ nút xác nhận) + `shared/dialog/dialog.service.ts` (`DialogService`, `providedIn: 'root'`, hai hàm `alert()`/`confirm(): Promise<boolean>` — nơi DUY NHẤT mở dialog, component không tự mở). Áp dụng NGAY vào xác nhận re-encode Hạng D (`startUpload()`) — nút xác nhận giờ tô màu warn đúng theo mockup A.2 mục 1, thay vì dialog gốc trình duyệt không theo theme.

`ng build`/`ng serve` (đã re-verify chunk chứa `DialogService`/`ConfirmDialog`/`uploadQueue`/`MatProgressBar`)/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch. `cargo build`/`cargo clippy` không đổi (round này thuần Angular). **CHƯA verify riêng lẻ redesign hai danh sách bằng `cargo tauri dev` thật** — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: verify thật Hạng C + mở khoá Hạng D (re-encode video)

**Verify thật (user tự chạy `cargo tauri dev` + tài khoản Telegram thật):** upload một file Hạng C THÀNH CÔNG — xác nhận đường ống remux (audio→AAC)+thumbnail+upload video+publish catalog của mục "wire Bắt đầu upload" (mục dưới) chạy đúng trên tài khoản thật, không chỉ trên `ng build`/lint sạch. Chi tiết từng bước (đúng % tiến trình, đúng phụ đề, đúng catalog ghim) CHƯA được xác nhận riêng lẻ — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12) giữ các dòng con đó ở trạng thái mở.

**Tinh chỉnh UI theo yêu cầu user:** nút hành động đổi tên "Bắt đầu upload (N)" → "Upload (N)", dời từ footer bảng metadata lên `.metadata-toolbar` (cùng hàng "Điền xuống"/"Đánh số tập tự động"/"Xoá khỏi hàng"), appearance đổi từ `mat-flat-button` sang `mat-raised-button` (nổi khối, đúng yêu cầu). Footer giờ chỉ còn "X/Y đã chọn" + trạng thái publish.

**Mở khoá Hạng D (theo yêu cầu user, sau khi Hạng C verify thành công):** thêm `reencode_to_mp4()` vào `ingest-ffmpeg` — decode+encode THẬT cả video (không chỉ audio như `remux()`) sang H.264 (`libx264`, preset `medium`, khớp `apps/tsmc-ingest/src/ffmpeg.ts::reencodeToMp4()`) + AAC. Khung video port gần nguyên văn ví dụ CHÍNH THỨC của chính crate `ffmpeg-next` (`examples/transcode-x264.rs`) — không tự chế pattern pts/dts mới cho phần rủi ro nhất (encode video, ADR-0013 § addendum 2026-09-03: "native FFI không có ranh giới tiến trình bảo vệ, một bug có thể segfault thẳng tiến trình" — rủi ro này TRƯỚC ĐÓ chỉ xác nhận ở audio, giờ áp dụng thêm cho video, lần đầu crate này gọi `avcodec_send_frame()` cho video). Khung audio giữ NGUYÊN cách dựng đã verify ở `remux.rs`/SPIKE-09 (không refactor dùng chung giữa hai file để tránh rủi ro đổi hành vi ở chỗ đã chứng minh đúng — chấp nhận trùng lặp ~60 dòng có chủ đích).

Verify TRỰC TIẾP bằng fixture AVI/Xvid+MP3 tổng hợp (sinh bằng `ffmpeg -f lavfi`, đúng profile Hạng D — container AVI luôn Hạng D theo ADR-0013 bất kể codec) qua ví dụ Rust chạy tay: `reencode_to_mp4()` chạy KHÔNG crash, output MP4/H.264/AAC đúng duration, `ffprobe -count_frames` đọc đủ khung hình, `ffmpeg -f null -` decode lại KHÔNG lỗi (đối chiếu độc lập với `ffprobe`) — TRƯỚC khi wire vào Tauri command, cùng kỷ luật đã áp dụng cho `remux()`/`extract_thumbnail()`/`extract_subtitles()`.

Wire: thêm `RemuxModeDto` (`copy`/`reencode_audio`/`reencode_all`, thay cho `reencode_audio_to_aac: bool` cũ) — `prepare_upload` giờ nhận mode này thay vì tự suy đoán, vẫn không tự quyết hạng (ADR-0017 điều kiện bắt buộc #4). Angular: `uploadableItems()` bỏ điều kiện loại Hạng D; `startUpload()` hỏi xác nhận bằng `window.confirm()` RIÊNG TỪNG FILE Hạng D trước khi gọi `prepareUpload(..., 'reencode_all', ...)` (mockup A.2 mục 1: "re-encode video luôn phải hỏi", kèm số phút ước tính `Math.max(1, Math.ceil(durationSec / 60))` — cần thêm field `durationSec` vào `QueueItem`, lấy từ `ProbeResultDto` lúc probe) — từ chối thì bỏ qua ĐÚNG file đó, batch vẫn chạy tiếp. Thêm stage `reencoding` (khác `remuxing`) hiện trong hàng đợi lúc xử lý Hạng D.

`cargo build`/`cargo clippy` (0 warning) + `ng build`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch. **CHƯA verify Hạng D bằng tài khoản Telegram thật** (chỉ verify pipeline ffmpeg cục bộ bằng fixture tổng hợp, chưa qua `cargo tauri dev` + file AVI thật của admin) — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12).

## 2026-09-12 — GUI ingest desktop: workspace ba vùng — wire "Bắt đầu upload" (Hạng A/B/C)

Đóng nốt 4 method `IngestRpc` còn lại thành Tauri command (`upload_video`/`upload_subtitle`/`publish_catalog` — `download_document` để dành màn "Trình quản lý catalog" sau) + toàn bộ pipeline remux/thumbnail/rút phụ đề mà slice trước còn thiếu. Phạm vi chốt theo lựa chọn user: **chỉ Hạng A/B/C** (Hạng D — cần re-encode video — bị chặn có chủ đích, đã mặc định bỏ chọn sẵn từ slice trước); **phụ đề đầy đủ như CLI** (cả nhúng trong container lẫn rời cạnh file).

**Rust — 3 hàm mới trong `ingest-ffmpeg`** (`remux()`/`extract_thumbnail()`/`extract_subtitles()`), port gần như nguyên văn từ `tools/spike-09/src/main.rs` (đã verify thật 72.1x realtime ở SPIKE-09/ADR-0013 § addendum 2026-09-03/04) — chỉ sửa 1 chỗ để qua `cargo clippy` (`clippy::never_loop` ở `extract_thumbnail`, đổi `while` thành `if` vì logic vốn chỉ lấy đúng 1 frame rồi thoát, hành vi không đổi). Verify trực tiếp bằng ví dụ chạy tay cả pipeline (probe → remux → re-probe → thumbnail → rút phụ đề) trên `tools/spike-09/fixtures/sample.mkv` thật — remux ra đúng MP4 H.264/AAC +faststart (102MB, khớp ~300s), thumbnail JPEG 6978 byte, phụ đề 2 cue — TRƯỚC khi wire vào Tauri command.

**Rust — 2 module command mới:** `pipeline.rs` (`prepare_upload` gộp remux+thumbnail+rút phụ đề thành MỘT lệnh chạy trong `spawn_blocking`, tự tạo/dọn thư mục tạm — giống `mkdtemp()`/`finally{rm}` của CLI nhưng phía Rust; bắn sự kiện `"pipeline-stage"` mỗi khi đổi bước, mockup A.2 mục 4) + `upload.rs` (`upload_video` bắn sự kiện `"upload-progress"` + nhận `CancelFlag` mới cho mỗi lần gọi — `AppState.active_cancel` giữ cờ của lần ĐANG chạy để `cancel_upload()` đọc lại; `upload_subtitle`/`publish_catalog` không cần tiến trình/huỷ). Thêm `list_dir_entries` (`probe.rs`) cho phụ đề rời — I/O thuần, logic so khớp tên file vẫn ở TypeScript (`matchSidecarSubtitles()`, ADR-0017 điều kiện bắt buộc #4). KHÔNG có chỗ nào tự quyết compat/hạng/reencode ở Rust — `reencode_audio_to_aac` (suy từ hạng đã `classifyCompatRank()`) và danh sách track phụ đề (đã lọc bỏ codec ảnh PGS/DVD) đều do Angular truyền vào.

**Angular (`workspace.ts`):** `startUpload()` xử lý TUẦN TỰ từng item đã chọn (probe lại lấy track phụ đề TEXT → `prepareUpload()` → `deriveCompat()` từ probe LẠI file đã remux → `uploadVideo()` → upload từng phụ đề nhúng + phụ đề rời tìm qua `listDirEntries()`/`matchSidecarSubtitles()`) rồi publish catalog ĐÚNG MỘT LẦN cho cả batch (đọc lại `readPinnedCatalog()` mới nhất, `mergeCatalogItems()`/`buildCatalogEnvelope()`, `@tsmc/core-ingest`). `withFloodWaitRetry()` bọc mọi RPC MTProto thật (upload video/phụ đề/publish) — bắt đúng `IngestRpcErrorDto.kind === 'FloodWait'`, đếm ngược đúng số giây rồi tự thử lại, không né bằng cách nào khác (CLAUDE.md). Nút Huỷ (icon vuông, thay thế nút xoá của đúng dòng đang upload) gọi `cancel_upload()` — RPC `upload_video()` đang chờ tự reject bằng `Cancelled`, item đó đánh dấu Lỗi, batch vẫn chạy tiếp cho các item còn lại (không dừng cả batch, cùng triết lý "một file lỗi không chặn batch" đã áp dụng từ bước probe). Vùng HÀNG ĐỢI hiện stage hiện tại thay rank badge lúc đang xử lý (`uploadStageLabel()`, kèm % byte cho bước upload video); footer bảng metadata thêm nút "Bắt đầu upload (N)" + trạng thái publish (đang chạy/lỗi/xong — `N item, M mới`).

`cargo build`/`cargo clippy` (0 warning) + `ng build`/`npm run lint`/`npm run test:libs`/`npm run docs:check` đều sạch. **CHƯA verify** upload thật bằng tài khoản Telegram (checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-bắt-đầu-upload-2026-09-12)) — đây là RPC MTProto thật đầu tiên của app này ngoài đăng nhập/chọn kênh, cần chạy tay theo CLAUDE.md ("không chạy đăng nhập MTProto hộ người dùng"). Còn để ngỏ, không chặn tiến độ: thanh chi phí (mockup "TRƯỚC KHI CHẠY"), Hạng D (re-encode video), remux song song upload (SPIKE-10 M6 để ngỏ có chủ đích), đóng gói DLL FFmpeg cho bản phân phối thật.

## 2026-09-12 — GUI ingest desktop: workspace ba vùng (A.3) — thêm vùng BẢNG METADATA

Tiếp ngay sau vùng HÀNG ĐỢI (mục dưới) trong cùng slice, theo lựa chọn tiếp tục của user. Bảng metadata (7 cột: checkbox/Tên file/Title/Season/Ep/Năm/hạng) dựng bằng `cdk-virtual-scroll-viewport` + input thường (KHÔNG dùng `mat-table`/`MatFormField` — giữ density compact theo mockup, cùng pattern div+CDK đã dùng ở vùng hàng đợi), sửa từng ô commit lúc blur (`(change)`, không phải mỗi phím gõ, tránh re-render thừa trong danh sách ảo hoá). Ba thao tác hàng loạt trên các dòng đã chọn (checkbox, có "chọn tất cả" ở header): "Điền xuống" (menu chọn Title/Season/Năm — lấy giá trị dòng ĐẦU TIÊN đã chọn gõ xuống các dòng còn lại), "Đánh số tập tự động" (Season của dòng đầu áp cho tất cả, Episode tăng dần từ Episode dòng đầu), "Xoá khỏi hàng". Item Hạng D tự động bỏ chọn checkbox lúc probe xong (mockup A.5).

Seed/kế thừa metadata dùng lại NGUYÊN VẸN `seedMetadataFromFilename()`/`inheritMetadata()` (`@tsmc/core-ingest`) — cùng logic tuần tự "kế thừa từ item ngay trước" mà `apps/tsmc-ingest/src/commands/upload.ts::resolveMetadataForFile()` đã verify thật ở ADR-0013, khác CLI đúng một điểm có chủ đích: CLI hỏi xác nhận (`confirm()`) từng file trước khi áp dụng kế thừa, còn ở đây không có dialog chặn luồng nào — kết quả kế thừa chỉ là GIÁ TRỊ GỢI Ý SẴN trong ô bảng (nguyên tắc UX A.2 mục 3: "không còn là một câu hỏi tuần tự chặn luồng"), admin tự sửa đè trực tiếp những dòng kế thừa sai (thường là lúc chuyển sang series/trailer khác giữa batch). Batch mới thả thêm (sau khi đã có hàng đợi từ trước) kế thừa tiếp từ dòng CUỐI hàng đợi hiện có, không seed lại từ đầu.

Phát hiện phụ lúc build: CSS component `workspace.scss` vượt ngân sách 4kB mặc định (`anyComponentStyle` budget) sau khi thêm layout hai cột + bảng — gộp 3 cặp `color-mix()` lặp lại thành CSS custom property (`--ws-border-8/12/20`) đưa về dưới ngân sách, không cần nới ngân sách trong `angular.json`. `ng build`/`ng serve`/`npm run lint` sạch — CHƯA verify bảng metadata bằng `cargo tauri dev` thật (chỉ vùng hàng đợi đã verify, xem mục dưới).

Tinh chỉnh UI theo yêu cầu user cùng ngày (hai vòng góp ý): header đổi từ `<header>` tự style sang `<mat-toolbar>` (bỏ CSS flex/border tự viết, dùng nguyên component Material), nút back ("Chọn kênh khác", điều hướng `/channel`, không xoá `SelectedChannelStore` — `Channel` tự quản state cục bộ, không đọc lại store lúc vào màn) đổi thành `mat-icon-button` (chỉ icon mũi tên trái, không kèm chữ) đặt NGOÀI CÙNG bên trái toolbar (khác bản đầu đặt bên phải kèm chữ). `.metadata-toolbar` (thanh "Điền xuống"/"Đánh số tập tự động"/"Xoá khỏi hàng") cũng đổi sang `<mat-toolbar>` cho nhất quán với header — thêm `height: auto` + `min-height: 56px` để không cắt mất nội dung khi các nút xuống dòng ở màn hẹp (mat-toolbar mặc định cố định chiều cao một dòng). Nút "Xoá khỏi hàng" đổi từ text sang `mat-icon-button` (icon thùng rác inline SVG, `color="warn"`, `aria-label`/`matTooltip` giữ nguyên chữ cho a11y) — icon inline theo đúng bất biến #8 (không nhúng font ligature).

Vòng góp ý thứ ba (cùng ngày): thêm ô tìm kiếm bên TRÁI `.metadata-toolbar` (đẩy 3 action button qua phải bằng spacer), lọc HIỂN THỊ theo tên file/Title, áp dụng đồng thời cho cả hai vùng (hàng đợi lẫn bảng metadata — hai cách nhìn của CÙNG một `queue`, lọc lệch nhau sẽ gây khó hiểu). Thao tác hàng loạt (điền xuống/đánh số/xoá/chọn tất cả) CỐ Ý đọc thẳng `queue()` không qua filter — tìm kiếm chỉ để định vị dòng, không thu hẹp phạm vi thao tác hàng loạt một cách bất ngờ. `{{ selectedCount() }}/{{ queue().length }} đã chọn` dời từ toolbar xuống một footer riêng dưới bảng, căn trái. Phát hiện phụ: thêm ô tìm kiếm khiến `workspace.scss` lại vượt ngân sách `anyComponentStyle` (4kB) — lần này gộp phần CSS input dùng chung (`.search-input`/mỗi ô bảng) bằng `%placeholder` + `@extend` (giảm lặp) VẪN không đủ, nên nới ngân sách cảnh báo lên 6kB trong `angular.json` (ngân sách lỗi giữ nguyên 8kB) — chấp nhận được vì đây là màn phức tạp nhất app (hai toolbar + bảng + hai vùng cuộn ảo hoá), không phải dấu hiệu CSS viết ẩu.

## 2026-09-12 — GUI ingest desktop: workspace ba vùng — verify thật vùng HÀNG ĐỢI bằng file season thật

User tự chạy `cargo tauri dev`, kéo lần lượt 5 file thật từ một season "The Big Bang Theory" (không phải folder) vào vùng HÀNG ĐỢI: 3 `.mkv` → 🟠C, 1 `.avi` (trailer) → 🔴D, 1 `.mp4` đã cắt đoạn → 🟡B — cả 5 khớp đúng kỳ vọng phân hạng, xác nhận `ingest-ffmpeg` + `classifyCompatRank()` chạy đúng trên file thật (khác fixture tổng hợp đã dùng lúc code). Có một `TypeError: Cannot read properties of undefined (reading 'startTime') at et.reportAllChanges` (`VM181:2`) hiện trên console nhưng không ảnh hưởng UI/thao tác — grep toàn bộ dist bundle + `node_modules` xác nhận chuỗi `reportAllChanges` không tồn tại ở đâu trong code/dependency của app, nghi ngờ đầu tiên là một devtools extension inject vào tiến trình WebView2, chưa xác nhận được nguồn cụ thể. Checklist cập nhật ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-hàng-đợi--probe-native-2026-09-12) — vẫn còn: kéo cả folder (season), file lỗi/không phải media, dedupe, nút xoá, reload trực tiếp vào `/workspace`, file rất lớn.

## 2026-09-12 — GUI ingest desktop: workspace ba vùng (A.3) — vùng HÀNG ĐỢI + probe native ffmpeg-next

Slice kế tiếp sau màn Chọn kênh, phạm vi thu hẹp có chủ đích (theo lựa chọn user): CHỈ vùng hàng đợi + phân hạng màu ngay khi thả file (nguyên tắc UX A.2 mục 2) — bảng metadata (điền xuống hàng loạt), thanh chi phí, và wire upload để dành slice sau. Crate Rust mới `ingest-ffmpeg` (thành viên thứ tư của Cargo workspace) — adapter probe bằng `ffmpeg-next` (native FFI, cùng lựa chọn đã verify ở [SPIKE-09](./spikes/README.md#spike-09)), đọc container/codec/kích thước/duration mà KHÔNG remux/encode/decode frame nào (không chạm đường code đã gây segfault ở [ADR-0013 § addendum 2026-09-03](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-03-spike-09--native-rust-ffi-cho-core-lib-tauri-tương-lai)) — verify trực tiếp bằng cách đối chiếu 1-1 với `ffprobe` CLI thật trên fixture `tools/spike-09/fixtures/sample.mkv` (container/codec/kích thước/duration khớp tuyệt đối). `classifyCompatRank()` (`@tsmc/core-ingest`) VẪN là nguồn sự thật duy nhất cho luật phân hạng A/B/C/D (ADR-0017 điều kiện bắt buộc #4) — `ingest-ffmpeg` chỉ sinh `ProbeResult`, không tự quyết hạng. 2 Tauri command mới KHÔNG đụng `AppState`/MTProto (`probe.rs`): `list_media_files` (quét đệ quy folder thả vào, lọc theo phần mở rộng video) + `probe_media` (probe MỘT file, lỗi trả riêng cho file đó — khớp mockup A.5 "Pipeline FFmpeg crash: file đó đánh dấu Lỗi, batch chạy tiếp", không gộp mảng).

UI: route `/workspace` mới (`ui/src/app/workspace/`) — kéo thả file/folder bằng sự kiện gốc webview (`getCurrentWebview().onDragDropEvent()`, KHÔNG thêm plugin dialog), `SelectedChannelStore` mới (service dùng chung giữa `/channel` và `/workspace` cho header "Kênh: ..."), `CdkVirtualScrollViewport` cho danh sách hàng đợi (bắt buộc theo A.3, dù chưa dài ở slice này). Phát hiện thật cùng lúc: thêm `@tsmc/core-ingest` vào `ui/package.json` làm `ng build` sạch nhưng `ng serve` (Vite) lỗi `Failed to resolve dependency: valibot` — transitive qua `@tsmc/shared-models` (dep của `core-ingest`), pnpm strict node_modules không hoist nó vào `ui/node_modules`; vá bằng cách khai `valibot` làm dependency TRỰC TIẾP của `ui/package.json`, đúng cách `apps/web/package.json` đã làm cho chính vấn đề này (tiền lệ có sẵn trong repo, không phải giải pháp mới bịa ra). `cargo build`/`cargo clippy`/`ng build`/`ng serve`/`npm run lint`/`npm run test:libs` đều sạch — CHƯA verify kéo thả bằng `cargo tauri dev` thật (checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--workspace-ba-vùng-hàng-đợi--probe-native-2026-09-12)), và đóng gói DLL FFmpeg cho bản phân phối thật vẫn để ngỏ (7 DLL, xem addendum ADR-0013 2026-09-04) — máy dev hiện có sẵn `vcpkg install ffmpeg:x64-windows` nên build/chạy được tại chỗ, chưa phải bằng chứng chạy được trên máy sạch.

## 2026-09-11 — GUI ingest desktop: sửa bug thật — nhánh auto-check nhanh không bao giờ lưu credentials, vòng lặp "vẫn phải gõ API_ID" lặp lại vô tận

Bug thật user report sau khi test: đóng/mở lại app vẫn phải gõ API_ID để "trigger session check" mới vào được, dù đã đăng nhập "thành công" nhiều lần trước đó. Xác nhận trực tiếp trên đĩa: `credentials.json` **không hề tồn tại** ở app-data dù `session.sqlite3` đã có từ lâu. Nguyên nhân: `runAutoCheck()` (nhánh debounce gõ-API_ID-rồi-tự-nhận-ra, `login.ts`) cố tình KHÔNG gọi `persistCredentials()` — quyết định lúc thêm tính năng (lo ngại lưu `api_hash`/phone rỗng làm hỏng prefill), nhưng hệ quả không lường trước: nhánh này NHANH HƠN HẲN nhánh "Tiếp tục" đầy đủ (không cần bịa API_HASH/số điện thoại hợp lệ định dạng), nên user luôn đăng nhập lại qua đúng nhánh này — `credentials.json` không bao giờ được tạo, `tryAutoLogin()` không bao giờ có gì để đọc lúc mở app tiếp theo. Vá bằng cách CŨNG `persistCredentials()` ở nhánh debounce khi `check_session()` thành công — an toàn vì `check_session()` vốn chỉ cần `api_id`, lưu sớm ở đây không mất gì (tái dùng giá trị hash/phone đã nhớ trước đó, rỗng nếu chưa từng có, không tệ hơn hiện trạng). Tiện sửa luôn thông báo lỗi `USER_RESTRICTED` (gặp khi test `create_channel` — tài khoản bị Telegram hạn chế tạo kênh, KHÔNG PHẢI bug của app) từ dạng kỹ thuật thô ("request error: rpc error 403...") sang câu tiếng Việt giải thích rõ đây là giới hạn thật từ Telegram, không có cách né. `cargo build`/`cargo clippy`/`ng build` sạch, build lại `.exe`.

## 2026-09-11 — GUI ingest desktop: danh sách/tìm kênh mở rộng sang supergroup

Theo yêu cầu user: `list_own_channels()`/`resolve_channel()` (Rust) trước đó chỉ chấp nhận `Peer::Channel` (broadcast) — mở rộng thêm supergroup (`Peer::Group` mà `raw` là `tl::enums::Chat::Channel`, cùng họ RPC `channels.*`/`InputPeer::Channel` với broadcast channel nên tương thích với `read_pinned_catalog()`/`upload_video()`/... hiện có). **Cố ý KHÔNG** mở rộng tới group nhỏ chưa nâng cấp supergroup — kiểu này dùng họ RPC `messages.*`/`InputPeer::Chat` hẳn khác, cho lọt vào sẽ vỡ muộn ("peer không phải InputPeer::Channel") ở bước publish thay vì báo ngay lúc chọn. Gộp logic phân loại vào một helper dùng chung `channel_like_creator()` (`ingest-grammers/src/rpc.rs`). Chi tiết: [ADR-0017 § Cập nhật tiếp trong cùng slice](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-11-ingestrpc-thêm-2-thao-tác-không-có-tương-ứng-1-1-phía-ts). `cargo build`/`cargo clippy`/`ng build` sạch, build lại `.exe`, chưa verify bằng tài khoản thật.

## 2026-09-11 — GUI ingest desktop: màn Chọn kênh đổi sang UI kiểu Telegram

Theo yêu cầu user: thay 3 khối tách rời (nút "Xem danh sách", form nhập ref riêng, form tạo kênh riêng) bằng một UI hợp nhất kiểu Telegram — danh sách kênh của admin tải NGAY lúc vào màn (không cần bấm), một ô lọc trên cùng vừa lọc tại chỗ theo tên (substring) vừa debounce 500ms thử `resolveChannel()` nếu gõ giống @username/link (kết quả hiện thêm một dòng "kết quả tìm kiếm" ngay trong cùng danh sách thay vì một form riêng), và nút "+" (icon button, SVG inline) cạnh ô lọc mở panel "Tạo kênh mới" gọn thay cho một khối cố định luôn hiện. Danh sách đổi từ `mat-list` + `button[mat-list-item]` sang `mat-action-list` (đúng directive Material cho danh sách bấm được). Bấm dòng "kết quả tìm kiếm" tái dùng THẲNG object đã resolve lúc debounce (không gọi `resolve_channel` lần hai — RPC đó đã tự ghi `state.selected_channel` phía Rust rồi). Tiện thể vá một gap phát hiện lúc viết checklist: kênh vừa tạo giờ được thêm tại chỗ vào danh sách hiển thị (`ownChannels.set([...])`) thay vì "biến mất" cho tới khi rời màn rồi vào lại (component không tự reload `list_own_channels()` khi quay về từ "result"). `ng build`/`cargo build`/`cargo clippy` sạch, build lại `.exe`, chưa verify bằng tài khoản thật (checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-chọn-kênh-2026-09-11) đã cập nhật theo UI mới).

## 2026-09-11 — GUI ingest desktop: màn Chọn kênh thêm picker + tạo kênh mới

Theo yêu cầu user: ngoài nhập ref trực tiếp, màn Chọn kênh giờ có thêm 2 đường — chọn từ danh sách kênh của admin, và tạo kênh media mới ngay tại app, đúng thiết kế gốc ở `docs/ux-design.md` § A.4 ("danh sách kênh ghi được") mà slice trước chưa làm được (trait `IngestRpc` lúc đó chỉ resolve được MỘT ref cụ thể). Thêm 2 method vào `IngestRpc` — `list_own_channels()` (quét `Client::iter_dialogs()`, lọc `is_own`) + `create_channel(title)` (raw invoke `channels.CreateChannel { broadcast: true, megagroup: false }`, cùng lời gọi TL `gateway-sync.ts` dùng cho kênh state, khác mục đích) — đây là 2 thao tác KHÔNG có tương ứng 1-1 phía TS, ngoại lệ có chủ đích ghi rõ ở [ADR-0017 § Cập nhật 2026-09-11](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-11-ingestrpc-thêm-2-thao-tác-không-có-tương-ứng-1-1-phía-ts). 3 Tauri command mới (`list_own_channels`, `create_channel`, `select_channel` — chọn một kết quả picker mà không resolve lại, peer cache Rust đã có sẵn entry từ lúc liệt kê) đưa tổng số command lên 12. UI (`channel.ts`/`.html`): 3 đường chọn kênh đều đổ vào một điểm chung `finishSelecting()` (check_write_permission → read_pinned_catalog); picker tải lười (bấm mới quét dialog, cache lại), form tạo kênh riêng. `cargo build`/`cargo clippy`/`ng build` sạch; build lại `.exe` bằng `cargo tauri build --no-bundle`, chưa verify bằng tài khoản thật.

## 2026-09-11 — GUI ingest desktop: nhớ credential ở app-data, mở app không cần gõ gì nếu còn đăng nhập

Bug thật phát hiện lúc verify bản release (mục dưới, "màn Chọn kênh"): bản đầu chỉ nhớ `api_id` ở `localStorage`, vốn tách riêng theo ORIGIN phục vụ webview — `cargo tauri dev` (`http://localhost:4300`) và bản release (protocol riêng của Tauri) là hai origin khác nhau, không chia sẻ gì. Kết quả: đổi qua lại giữa hai cách chạy khiến user bị bắt gõ lại cả API_ID + API_HASH + số điện thoại mỗi lần, dù `check_session()` chỉ dùng `api_id` và session MTProto thật vẫn còn nguyên (không mất gì, chỉ mất tiện nghi tự điền). Sửa đúng gốc: chuyển việc nhớ credential từ `localStorage` sang `credentials.json` ở app-data (2 command mới `load_saved_credentials()`/`save_credentials()`, `commands.rs`/`dto.rs`) — thư mục này KHÔNG phụ thuộc origin webview, cùng chỗ với `session.sqlite3`. `tryAutoLogin()` (`login.ts`) giờ đọc đủ cả 4 trường (API_ID/API_HASH/mã vùng/số điện thoại), tự `check_session()` ngay trong constructor — còn hợp lệ thì nhảy thẳng KHÔNG GÕ GÌ, hết hạn thì Bước 1 tự điền sẵn cả 4 ô thay vì trống trơn. Giữ thêm một lớp phòng thủ debounce (`scheduleAutoCheck()`) cho đúng trường hợp `credentials.json` chưa từng tồn tại (bootstrap lần đầu tuyệt đối) — gõ xong API_ID tự thử `check_session()` ngầm, không đợi 2 ô kia. Cũng gỡ gate `cfg!(debug_assertions)` khỏi `tauri_plugin_log` ở `lib.rs` — bản release trước đó không ghi log gì, không debug được gap này khi mới xảy ra. `cargo build`/`cargo clippy`/`ng build` sạch; build lại `.exe` bằng `cargo tauri build --no-bundle`, chưa verify bằng tài khoản thật (checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-chọn-kênh-2026-09-11)).

## 2026-09-11 — GUI ingest desktop: màn Chọn kênh (A.4) — code xong, chưa verify thiết bị thật

Slice kế tiếp sau màn Đăng nhập. Rust (`src-tauri`): thêm `AppState.selected_channel` (nhớ channel của lần `resolve_channel()` gần nhất — `GrammersIngestRpc::peer_for()` tra `Peer` từ cache nội bộ theo `channel.id`, chỉ có dữ liệu sau một `resolve_channel()` thành công trong cùng phiên `Ready`) + 2 Tauri command mới không nhận tham số, đọc lại `selected_channel`: `check_write_permission`, `read_pinned_catalog` (trả `PinnedCatalogDto` mới ở `dto.rs`). Tổng 3/7 method `IngestRpc` giờ có UI gọi (`resolve_channel` đã wire từ khung sườn, chưa có UI tới slice này). UI (`ui/src/app/channel/`): route `/channel` mới, form nhập username/invite-link (không có picker danh sách kênh như mockup gốc — trait `IngestRpc` không có RPC "liệt kê kênh ghi được", chỉ resolve một ref cụ thể mỗi lần) — chặn ID thô tại form (bất biến #10, bản sao có chủ đích của `looksLikeRawId()` ở `apps/web/src/app/sources/add-source-sheet/`, hai app không dùng chung lib UI), chặn chọn kênh `is_own === false` (bất biến #5, dừng ngay sau `resolve_channel`, không gọi tiếp 2 RPC còn lại), đếm số `items[]` của catalog đã ghim bằng parse phòng thủ (`tryDescribeCatalog()` — không throw khi JSON hỏng/sai `spec`, KHÔNG phải schema validation Valibot đầy đủ như `libs/shared-models/src/catalog.ts` dùng cho `apps/web`, việc đó để dành màn "Trình quản lý catalog" sau). Ref đã dùng nhớ ở `localStorage` để prefill (KHÔNG tự động resolve lại lúc mở app — khác `api_id` ở login, vì peer cache phía Rust không sống qua lần khởi động lại nên "nhớ" không tiết kiệm được RPC nào). Login (`login.ts`) đổi panel "Đã đăng nhập" tĩnh thành điều hướng thật sang `/channel` (`goToDone()`). `cargo build --workspace`/`cargo clippy --workspace` (0 warning) + `ng build` sạch — CHƯA verify qua `cargo tauri dev` + tài khoản Telegram thật (CLAUDE.md: không chạy đăng nhập MTProto hộ người dùng). Checklist: [docs/pending-device-tests.md § Chọn kênh](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-chọn-kênh-2026-09-11).

## 2026-09-10 — GUI ingest desktop: màn Đăng nhập tự nhận ra session cũ lúc mở app

Gap phát hiện lúc verify thiết bị thật (mục ngay dưới): `check_session()` khôi phục đúng, nhưng UI không tự gọi lúc mở app — user phải tự gõ lại cả 3 ô Bước 1 mỗi lần. Vá bằng `tryAutoLogin()` (`ui/src/app/login/login.ts`) — nhớ `api_id` (KHÔNG nhớ `api_hash`/số điện thoại) ở `localStorage`, tự gọi `check_session()` một lần trong constructor: có session hợp lệ → nhảy thẳng panel "Đã đăng nhập" qua trạng thái `'checking'` mới (mirror `status 'checking'` của `apps/web/src/app/login/login.ts`); không có gì nhớ, hoặc session cũ chết/hết hạn → im lặng rơi về Bước 1 (API_ID được điền sẵn nếu có), không chặn onboarding bằng lỗi. `pnpm run build`/lint sạch; verify bằng Puppeteer headless hai kịch bản (không `localStorage` → Bước 1 trống; có `localStorage` giả → `checkSession()` lỗi ngoài môi trường Tauri như dự kiến, rơi về Bước 1 với API_ID điền sẵn, không crash) — KHÔNG chạm Tauri/MTProto thật. Đóng nốt mục "UI Đăng nhập chưa tự động kiểm tra session" ở [docs/roadmap.md § Ingest](./roadmap.md#ingest).

## 2026-09-10 — GUI ingest desktop: verify đăng nhập thật qua UI mới — ĐẠT, kể cả khôi phục session (lần đầu, không 2FA)

Admin tự chạy `cargo tauri dev` với `ui/` Angular mới trên máy thật, đăng nhập bằng tài khoản Telegram thật (không bật 2FA): `devUrl` cổng 4300 nạp đúng màn Đăng nhập, Bước 1 (API_ID/API_HASH/số điện thoại) → `check_session` + `request_login_code` → Bước 2 (OTP) → `submit_otp` trả `LoggedIn` → panel "Đã đăng nhập" — đúng thiết kế A.4. Đóng app, mở lại, nhập lại đúng 3 ô ở Bước 1 (API_ID cũ + API_HASH + số điện thoại — UI không tự động kiểm tra lúc mở app, xem gap ghi ở [docs/roadmap.md § Ingest](./roadmap.md#ingest)) rồi bấm "Tiếp tục" → `check_session` lần hai trả `true`, nhảy thẳng panel "Đã đăng nhập", KHÔNG hỏi lại OTP — khớp DB thật: `dc_home` = 5, `dc_option` có 2 dòng (DC 2 ban đầu + DC 5 sau migrate lúc `request_login_code`), đúng dòng DC 5 có `auth_key`. Xác nhận cơ chế persist session của `grammers-session` `SqliteSession` (kể cả qua migrate DC) hoạt động đúng ở đường dẫn app-data của Tauri, không chỉ ở cwd như CLI đã verify tại SPIKE-10. Gỡ hết case đã test khỏi [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-đăng-nhập-ui-thật-2026-09-10). Nhánh 2FA (Bước 3) và case OTP sai vẫn chưa test (tài khoản dùng để verify không bật 2FA).

## 2026-09-10 — GUI ingest desktop: màn Đăng nhập UI thật (Angular 22 + Material)

Thay `ui/index.html` (trang thuần HTML/JS chứng minh IPC, xem mục khung sườn ngay dưới) bằng một app Angular 22 zoneless standalone riêng, `ui/` (`@tsmc/tsmc-ingest-desktop-ui`, thêm vào `pnpm-workspace.yaml`) — KHÔNG chung project Angular với `apps/web`, chỉ chung pnpm workspace/lockfile. Màn Đăng nhập khớp [docs/ux-design.md § Phụ lục A.4](./ux-design.md#a4-các-màn-còn-lại): `MatStepper` vertical 3 bước (API Credential → Mã xác nhận → Mật khẩu 2FA), cùng hình dạng/convention với `apps/web/src/app/login` (F1.1) — cảnh báo ADR-0011 §5 hiện trước ô nhập đầu tiên, `[linear]="false"` + `[editable]="false"` từng step (né đúng race điều kiện signal×CdkStepper đã ghi ở ADR-0016 addendum 2026-08-27), icon `<mat-icon>` mặc định thay bằng SVG inline (bất biến #8). Một cổng IPC duy nhất `ui/src/app/core/ingest-rpc.ts` bọc `invoke()` của `@tauri-apps/api`, kiểu TypeScript khớp nguyên văn serde output của `src-tauri/src/dto.rs` (kể cả field snake_case như `is_own`, khác quy ước camelCase của tham số `invoke()`). Hai phát hiện lúc code, không phải bug: (1) backend reset `ConnState::Disconnected` khi OTP/mật khẩu sai (xem README) khiến `request_login_code` gọi lại báo lỗi "gọi check_session() trước" nếu UI chỉ lùi về Bước 1 mà không làm gì thêm — vá bằng cách UI tự gọi lại `check_session()` ngầm bằng `api_id` đã biết trước khi cho bấm lại; (2) `FLOOD_WAIT` giữ nguyên số giây tới UI (không collapse về string) và hiện đếm ngược sống, khớp nguyên tắc UX A.2 mục 4 dù đây mới là màn Đăng nhập, chưa phải hàng đợi upload của A.3. `pnpm --filter @tsmc/tsmc-ingest-desktop-ui run build` sạch; verify bằng Puppeteer headless nhắm `ng serve` (KHÔNG phải cửa sổ Tauri, KHÔNG chạm MTProto thật — CLAUDE.md cấm agent tự chạy đăng nhập): stepper render đúng, lock-hint Bước 2 phản ứng đúng theo `credentialsValid`, lỗi `invoke()` ngoài môi trường Tauri (không có `window.__TAURI_INTERNALS__`) bị bắt gọn bởi `toIngestRpcError()`/`describeIngestError()`, không crash trang. **Chưa verify**: đăng nhập thật qua UI mới bằng tài khoản Telegram thật, `cargo tauri dev` nạp đúng `devUrl` cổng 4300 — checklist cập nhật ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-đăng-nhập-ui-thật-2026-09-10).

## 2026-09-10 — GUI ingest desktop: khung sườn `apps/tsmc-ingest-desktop` (Tauri + grammers-client)

Sau khi SPIKE-9/10 đóng và ADR-0017 chọn `grammers-client`, cập nhật `docs/ux-design.md`/`docs/roadmap.md` cho đúng thứ tự "Plan sau spike" rồi dựng khung sườn thật: Cargo workspace 3 crate (`ingest-rpc-trait` — trait `IngestRpc` ported từ `tools/spike-10/rpc-trait`; `ingest-grammers` — implementation ported từ `tools/spike-10/r3-grammers/src/{rpc,session}.rs`; `src-tauri` — Tauri v2 backend). 5 Tauri command wire thật: 4 command bootstrap đăng nhập (`check_session`/`request_login_code`/`submit_otp`/`submit_password`, tách state machine nhiều bước vì webview không có stdin để chặn chờ OTP như CLI) + `resolve_channel` (1/7 method của `IngestRpc`, đủ để chứng minh đường IPC → Rust → MTProto chạy đúng). 6 method còn lại (upload video/subtitle, publish catalog, đọc catalog ghim, tải document, kiểm tra quyền ghi) đã có implementation đầy đủ nhưng chưa wire — để dành slice UI thật (mockup A.3, `ux-design.md` § Phụ lục A). `cargo build --workspace`/`cargo clippy --workspace` sạch (0 warning); `cargo tauri dev` boot cửa sổ WebView2 thật, chạy ổn định không panic. Một bug môi trường lặp lại đúng như SPIKE-10 đã ghi (`grammers-crypto`/`num-bigint` xung đột qua `glass_pumpkin`, vá bằng `cargo update -p glass_pumpkin --precise 2.0.0-rc0`); một phát hiện thật mới (login flow CLI chặn stdin không port được nguyên vẹn cho webview, phải tách state machine). Chưa verify: đăng nhập + resolve kênh bằng tài khoản Telegram thật — checklist [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-đăng-nhập-ui-thật-2026-09-10). Chi tiết: [ADR-0017 § Cập nhật 2026-09-10](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-10-khung-sườn-appstsmc-ingest-desktop--code-thật-lần-đầu).

## 2026-09-04 — SPIKE-09: tích hợp thật vào `apps/tsmc-ingest` + sửa số liệu "Đóng gói"

Nối `apps/tsmc-ingest` gọi được `spike09.exe` thật qua backend chọn được (`TSMC_INGEST_FFMPEG_BACKEND=native`, mặc định vẫn shell-out — không đổi hành vi production/ADR-0013): `ffmpeg-native.ts`/`ffmpeg-backend.ts` mới, `upload.ts` đổi 1 dòng import. Admin chạy thật `upload` trên file AVI thật (Hạng D) với backend native — lộ ra số liệu "6 DLL" của SPIKE-09 hôm trước **sai** (thiếu `swresample-5.dll`, phụ thuộc transitive của `avcodec-61.dll` mà `objdump` trên chính `spike09.exe` không thấy được): đúng là **7 DLL, ~21.3 MB**, và đủ 7 file thì tự chứa hoàn toàn (không cần `PATH`). Cũng sửa 2 bug code lộ ra cùng lần chạy: thông điệp lỗi đoán nhầm "có thể segfault" cho một lỗi DLL_NOT_FOUND thật (đã thêm `describeWindowsExitCode()` phân biệt `0xC0000135` DLL_NOT_FOUND vs `0xC0000005` ACCESS_VIOLATION thật), và nhãn timing log sai backend cho nhánh Hạng D (luôn shell-out, không phụ thuộc biến môi trường). Chi tiết: [ADR-0013 § Cập nhật 2026-09-04](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-04-tích-hợp-thật-vào-tsmc-ingest--sửa-số-liệu-đóng-gói-ở-addendum-trên), [SPIKE-09](./spikes/README.md#spike-09).

## 2026-09-03 — SPIKE-09: native Rust FFI (ffmpeg-next) cho core lib Tauri tương lai — ĐẠT, có caveat

Dựng thật `tools/spike-09/` (crate Rust độc lập, `ffmpeg-next` + FFmpeg 7.1.1 qua `vcpkg`) trên máy Windows 11 thật, cài đủ 3 việc `tsmc-ingest` CLI cần (remux copy-video + encode-audio AAC + faststart, thumbnail JPEG, rút subtitle `.srt`). Kết quả: 72.1x realtime (file mẫu tổng hợp, không phải file thật baseline 40.8x), 6 DLL ~21.2 MB, build crate 14.2s (toolchain vcpkg/LLVM đã có sẵn từ trước, chưa đo chi phí cài máy sạch). **Phát hiện quan trọng hơn số liệu:** một lỗi tầm thường (buffer audio sai số kênh) làm `avcodec_send_frame()` segfault thẳng tiến trình thay vì trả `Result::Err` — rủi ro loại khác hẳn shell-out CLI hiện tại (lỗi tương đương ở đó chỉ làm tiến trình con thoát mã lỗi, CLI cha vẫn sống). Quyết định GUI Tauri vẫn để ngỏ; điều kiện áp dụng nếu chọn hướng native FFI ghi ở [ADR-0013 § Cập nhật 2026-09-03](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-03-spike-09--native-rust-ffi-cho-core-lib-tauri-tương-lai). Chi tiết đầy đủ: [SPIKE-09](./spikes/README.md#spike-09).

## 2026-08-31 — Player: verify thật phụ đề (đơn ngôn ngữ) — ĐẠT

Verify bằng tài khoản Telegram thật trên staging: phát video kèm phụ đề — menu CC gốc của `<video controls>` hiện đúng track, bật lên hiện đúng nội dung/thời điểm, xác nhận cả chuỗi RPC `getSubtitleDocument` (Core Worker) lẫn convert SRT→VTT (`subtitle-convert.ts`) hoạt động đúng với dữ liệu thật. **Case nhiều phụ đề cùng lúc (đa ngôn ngữ) chưa test — hoãn có chủ đích**, gộp chung vào khi làm UI chọn track riêng thay vì test rời ngay bây giờ. Đóng phần lớn checklist ở [docs/pending-device-tests.md § Player: hiển thị phụ đề](./pending-device-tests.md#player-hiển-thị-phụ-đề-subs-2026-08-31) (còn hai case nhỏ chưa test: item không có `subs[]`, mở lại player nhiều lần liên tiếp).

## 2026-08-31 — Player: đọc/hiển thị `subs[]` — code xong, chưa verify thiết bị thật

Đóng gap ghi nhận từ lần verify ingest 2026-08-30 ("video phát được nhưng phụ đề không hiện"). Core Worker thêm RPC `getSubtitleDocument(sourceId, msgId, correlationId)` (`libs/worker-host/src/core-worker.ts`) — tải trọn document phụ đề một lần qua `downloadEngine.getInfo()` + `fetchWindow(offset=0, windowSize=size)`, khác hẳn `fetchChunk()` của video (không cần windowing/Range cho file vài trăm KB). `apps/web/src/app/player/player.ts` gọi RPC này song song với `checkCompat()` cho từng phần tử `subs[]`, convert SRT→VTT bằng hàm thuần mới `subtitle-convert.ts` (chỉ thêm khai báo `WEBVTT` + đổi dấu phẩy→chấm trong timestamp — dòng số thứ tự cue của SRT vẫn hợp lệ làm cue identifier VTT), rồi gắn `<track kind="subtitles">` bằng Blob URL (revoke lúc component huỷ, `DestroyRef`). Một track lỗi (message bị xoá, FLOOD_WAIT...) bị bỏ qua độc lập, không chặn track khác hay việc phát video. Không tự vẽ UI chọn/bật-tắt track — để nguyên cho menu CC gốc của `<video controls>` (giữ đúng tinh thần ADR-0005 phương án C, chỉ mở rộng thêm đúng phần trình duyệt không tự làm được: tải một message rời qua MTProto). `npm run lint`/`npm run build:web`/`npm run test:libs` (291/291) sạch, đã `npm run deploy:staging` — CHƯA verify bằng phụ đề thật trên thiết bị thật (timing sau convert, hiển thị trong menu CC, nhiều track cùng lúc). Checklist: [docs/pending-device-tests.md § Player: hiển thị phụ đề](./pending-device-tests.md#player-hiển-thị-phụ-đề-subs-2026-08-31).

## 2026-08-30 — `tsmc-ingest` CLI — verify thật Hạng D + phụ đề ngoài — ĐẠT

Upload thật file AVI (Hạng D) kèm sidecar `.srt` cùng thư mục: phân hạng đúng D, hỏi xác nhận re-encode, `libx264` chạy thật (33.5x, ~127 MB cho ~24 phút), phụ đề ngoài được tự phát hiện + upload + ghi đúng `subs: [{ lang: "und", msgId: 19 }]`. Đóng nốt phần "Hạng D chưa verify" của addendum trước (A/B vẫn hoãn có chủ đích). Quan sát không phải bug: video phát được nhưng phụ đề không hiện — `apps/web` player chưa có code đọc `subs[]`, ghi nhận gap mới ở [docs/roadmap.md § UI theo từng màn hình](./roadmap.md#ui-theo-từng-màn-hình). Checklist cập nhật ở [docs/pending-device-tests.md](./pending-device-tests.md#kết-quả-verify-2026-08-30-hạng-d--phụ-đề-ngoài).

## 2026-08-30 — `tsmc-ingest` CLI — phụ đề ngoài (sidecar) tự dò cạnh video + README

Sau khi nối dây upload phụ đề NHÚNG (mục dưới cùng ngày), user chỉ ra README mới viết không có cách nào chỉ định phụ đề trên dòng lệnh — nhiều file thực tế có phụ đề dưới dạng `.srt` RỜI đặt cạnh video, chưa được xử lý. Thêm tự động dò theo quy ước Plex/Jellyfin/Kodi (`<tên video>.srt` hoặc `<tên video>.<lang>.srt`, cũng nhận `.vtt`) — không thêm flag CLI mới (batch nhiều video một lệnh khiến một flag `--subs` không rõ ứng với video nào). Logic thuần so khớp tên file ở `libs/core-ingest/src/sidecar-subtitles.ts` (8 test mới), I/O (`readdir`) ở `apps/tsmc-ingest/src/sidecar-subtitles.ts`, wire vào `commands/upload.ts` — gộp chung `subs[]` với phụ đề nhúng. 291/291 test qua (tăng từ 283), lint/tsc/build sạch — CHƯA verify thật. Cũng thêm `apps/tsmc-ingest/README.md` (chưa từng có) — hướng dẫn cài đặt, ba lệnh, bảng phân hạng A/B/C/D, hai nguồn phụ đề, troubleshooting. Chi tiết: [ADR-0013 § Cập nhật 2026-08-30, phụ đề ngoài — sidecar](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-08-30-phụ-đề-ngoài--sidecar--tự-dò-file-srtvtt-cạnh-video).

## 2026-08-30 — `tsmc-ingest` CLI — re-verify bản vá `series.name` bằng tài khoản thật — ĐẠT

Upload thêm file thứ ba cùng series (`S01E08.mkv`, chọn "kế thừa") sau khi build lại CLI với bản vá `series.name` (xem mục ngay dưới cùng ngày) — `catalog.v1.json` thật ra đúng `series.name: "The big bang theory"` (khớp title), không còn lỗi ra chuỗi filename trần trụi. Catalog vẫn gộp đủ 5 item qua nhiều lần publish liên tiếp. Đóng nốt mục "Còn thiếu" cuối cùng của lần verify Hạng C — checklist đã gỡ ở [docs/pending-device-tests.md](./pending-device-tests.md#tsmc-ingest-cli--loginprobeupload-thật-2026-08-29). Chi tiết: [ADR-0013 § Cập nhật 2026-08-30, re-verify series.name — ĐẠT](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-08-30-re-verify-bản-vá-seriesname-bằng-tài-khoản-thật--đạt).

## 2026-08-30 — `tsmc-ingest` CLI — nối dây subtitle upload + mở SPIKE-09 (native Rust FFmpeg binding)

Phụ đề text (`.srt`) rút ra bằng `extractSubtitles()` (Hạng C) giờ được upload thành document rời (`uploadSubtitleDocument()` mới, `libs/core-mtproto/src/gateway-ingest.ts`, `forceDocument: true`) và ghi vào `subs: [{ lang, msgId }]` của catalog item — trước đây chỉ lưu cục bộ rồi bỏ. Phụ đề ảnh (`.sup`, PGS) vẫn giữ nguyên hành vi cũ (chỉ lưu cục bộ). Brainstorm cùng ngày quyết định hoãn có chủ đích việc verify Hạng A/B/D bằng file mẫu thật (biến thể của cùng cơ chế đã verify ở Hạng C) để ưu tiên gap này. `npm run lint`/`tsc --noEmit`/`npm run test:libs` (283/283)/`npm run build:ingest` đều sạch — CHƯA verify upload subtitle thật bằng tài khoản Telegram, checklist ở [docs/pending-device-tests.md](./pending-device-tests.md#tsmc-ingest-cli--loginprobeupload-thật-2026-08-29). Chi tiết: [ADR-0013 § Cập nhật 2026-08-30, nối dây subtitle upload](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-08-30-nối-dây-subtitle-upload--phụ-đề-text-không-còn-bị-bỏ-phí).

Cũng mở [SPIKE-09](./spikes/README.md#spike-09) (chưa dựng): thử nghiệm `ffmpeg-next` (native Rust FFI) có đáng thay thế shell-out `ffmpeg`/`ffprobe` hiện tại cho một core lib Tauri tương lai không — theo yêu cầu user "thử trước, đánh giá sau", có fallback rõ ràng về shell-out nếu native không đáng công sức. Quyết định "có xây Tauri GUI hay không" vẫn để ngỏ như ADR-0013 gốc, spike này chỉ trả lời câu hỏi cơ chế gọi FFmpeg.

## 2026-08-30 — `tsmc-ingest` CLI — verify Hạng C bằng tài khoản/kênh thật lần đầu

`probe`/`upload` chạy thật trên file MKV/H.264/AC3 thật (kênh `tsmc_mediacenter`): phân hạng đúng Hạng C, remux (copy video + encode audio AAC) qua ffmpeg thật (~33s/~22 phút nội dung, 40.8x), prompt metadata hoạt động, upload thành công (`msgId 3`), `compat` suy đúng "full" sau remux, `catalog.v1.json` ghim đúng — xác nhận bằng Telegram app thật. Cũng sửa hai bug lộ ra khi chạy thật lần đầu (không phải unit test bắt được): CLI treo vô thời hạn sau khi xong việc (GramJS giữ kết nối MTProto mở, event loop Node không tự rỗng — `cli.ts` giờ `process.exit()` khi `main()` xong), và thêm đọc `TSMC_PHONE_NUMBER` tuỳ chọn từ `.env` để bỏ qua prompt số điện thoại lúc test lặp lại. Test tiếp file thứ hai cùng series (`S01E02`) xác nhận kế thừa season/episode + gộp catalog (3 item, không mất item cũ) đều đúng — nhưng lộ bug thật: `series.name` bị ghi đè bằng chuỗi tên file trần trụi (`"S01E02.mp4"`) thay vì tên phim, do `inheritMetadata()` lấy nguyên `series` từ file mới thay vì chỉ lấy season/episode, cộng với việc sửa Title qua prompt không đồng bộ lại `series.name`. Đã vá cả hai chỗ + thêm 2 unit test tái hiện bug (282/282 qua) — CHƯA re-verify bản vá bằng tài khoản thật. Còn thiếu: mẫu Hạng A/B/D thật, re-verify kế thừa metadata sau vá — checklist ở [docs/pending-device-tests.md](./pending-device-tests.md#kết-quả-verify-2026-08-30-hạng-c-lần-đầu-tiên-trên-tài-khoảnkênh-thật).

## 2026-08-29 — `tsmc-ingest` CLI (ADR-0013 mục 1) — lần code đầu tiên

CLI mới `apps/tsmc-ingest` (Node, esbuild → `dist/cli.js`): `login` (phone/2FA qua terminal), `probe <file...>` (ffprobe + phân hạng A/B/C/D, dry run), `upload --channel <ref> <file...>` (probe → remux/re-encode → thumbnail → metadata kế thừa từ tập trước cùng series → upload → publish `catalog.json` một lần/batch). Logic thuần (phân hạng, kế thừa metadata, gộp catalog) ở lib mới `libs/core-ingest`, test bằng fixture — không cần ffprobe/ffmpeg thật lúc `test:libs`. `libs/core-mtproto` thêm `gateway-ingest.ts` (`uploadVideoDocument()`, việc mới duy nhất — `publishCatalogDocument()` tái dùng nguyên vẹn) và tách `SessionStoragePort` khỏi Dexie để CLI tự lưu session mã hoá ở `~/.tsmc-ingest/session.local.json`. Đã verify lint/tsc/test:libs (280/280)/build CLI thật; CHƯA verify login/probe/upload bằng tài khoản Telegram + ffmpeg thật — checklist ở [docs/pending-device-tests.md](./pending-device-tests.md#tsmc-ingest-cli--loginprobeupload-thật-2026-08-29). Chi tiết phát hiện lúc code (esbuild bundle Node, key extractable...): [ADR-0013 § Cập nhật 2026-08-29, lần code đầu tiên](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-08-29-tsmc-ingest-cli--lần-code-đầu-tiên).

## 2026-08-29 — Index: category theo Forum Topic + hashtag làm tín hiệu fallback

Quét lịch sử (T2/T3) giờ suy ra `topic` (tên Forum Topic, `channels.GetForumTopics`, cache 1h/kênh — file mới `libs/core-index/src/forum-topics.ts`) và hợp nhất hashtag (`message.entities`) với fallback tên file (file mới `libs/core-index/src/hashtag-parser.ts`, tách khỏi `parseFilenameFallback()` cũ) — hashtag ưu tiên trước filename cho season/episode, filename luôn thắng cho title, hashtag lạ gộp vào `genres`. `docs/catalog-spec.md` cập nhật field `topic` mới. Chưa verify bằng kênh Forum thật (khác SPIKE-07 vốn verify bằng script test riêng) — chi tiết: [ADR-0010 § Cập nhật 2026-08-29](./adr/0010-catalog-spec-v1-va-chien-luoc-indexing.md#cập-nhật-sau-khi-accepted-2026-08-29-spike-07--brainstorm-cải-thiện-quét-nguồn).

## 2026-08-29 — Redesign kiểu Netflix

Browse đổi từ list sang lưới card (CDK virtual scroll virtualize theo hàng), tap card mở `ItemDetailSheet`. Collections tách 2 tầng: danh sách tile (`home/collections`) → chi tiết một bộ sưu tập (`home/collections/:id`, route con mới). Sources: `AddSourceSheet` đổi từ toggle-mở-rộng-tại-chỗ sang 3 bước điều hướng trong cùng một sheet. Toolbar chung (`MainShell`) thay cho toolbar riêng của Browse. Chi tiết mockup/rationale từng màn: [docs/ux-design.md](./ux-design.md).

## 2026-08-28 — Ingest Editor (Màn hình 6) + SPIKE-06

Route `/metadata/:sourceId/:msgId`: sửa title/năm/compat rồi ghi đè `catalog.json` thật lên kênh media (`publishCatalogDocument()`), chỉ hợp lệ cho Kho Cá Nhân (`checkSourceWritable()`). SPIKE-06 đóng — kiểm chứng bằng tài khoản thật: publish lần đầu, publish-cập-nhật, đọc lại byte-chính-xác đều đạt; mã nguồn spike đã xoá sau khi đóng, số liệu giữ ở [docs/spikes/README.md](./spikes/README.md#spike-06). Chi tiết ranh giới quyền/rủi ro chưa kiểm chứng: [ADR-0014 addendum, 2026-08-28 SPIKE-06](./adr/0014-mo-hinh-kenh-media-dung-chung-state-rieng-tu.md#cập-nhật-sau-khi-accepted-2026-08-28-spike-06).

## 2026-08-28 — Settings (Màn hình 7) + Player (Màn hình 5)

Settings: khối Tài khoản (đăng xuất đúng Logout Journey — flush outbox → dừng timer → `auth.LogOut` → xoá IndexedDB, dừng ngay ở bước lỗi), Lưu trữ, Mạng (`setMaxConcurrency()` 4↔8), Debug. Chi tiết trần song song: [ADR-0006 addendum, 2026-08-28 slice Settings](./adr/0006-download-pipeline-dc-pool-flood-wait.md#cập-nhật-sau-khi-accepted-2026-08-28-slice-settings--màn-hình-7). Player: `compat-warning-sheet.ts` chặn phát khi `compat` là `partial`/`unplayable`, `flood-wait-notice.ts` bắc cầu tín hiệu `FloodWaitTooLongError` sang `MatSnackBar`.

## 2026-08-28 — Browse/Collections/Sources mobile-first (Màn hình 2/3/4)

Browse: thanh tìm kiếm + chip lọc nguồn cố định trên cùng. Collections: CRUD thật (tạo/đổi tên/xoá, kéo-thả sắp xếp qua op `collection.reorder` mới). Sources: thẻ nguồn thật (tier/số phim/lỗi quét) + `AddSourceSheet` (`MatBottomSheet`, chặn dán ID thô theo ADR-0014 §1) + gỡ nguồn.

## 2026-08-27 — UI Login mobile-first (Màn hình 1) + skeleton routing

Banner cảnh báo + Vertical `MatStepper` theo đúng Màn hình 1. Route `login` (full-bleed) → `home` (gate qua `authGuard`, `MainShell` bottom nav tự ghép) → `player/...` (immersive). Phát hiện icon font ligature + MatStepper race: [ADR-0016 addendum, 2026-08-27](./adr/0016-angular-material-va-cdk.md#cập-nhật-sau-khi-accepted-2026-08-27-slice-ui-login).

## 2026-08-26 — Playback (F4) end-to-end + hardening

SW giả lập HTTP Range, tải chunk qua MTProto, phát video thật (tài khoản thật). Sau đó slice hardening bổ sung AIMD + circuit breaker theo DC + CDN redirect (AES-CTR + verify SHA-256). SPIKE-04 đóng (chấp nhận rủi ro — ~1.1 GB tải liên tục ở mức 4/8 request, 0 lần FLOOD_WAIT). Chi tiết: [ADR-0006 addendum, 2026-08-26 slice hardening](./adr/0006-download-pipeline-dc-pool-flood-wait.md#cập-nhật-sau-khi-accepted-2026-08-26-slice-hardening--aimd--circuit-breaker--cdn-redirect).

## 2026-08-26 — Browse (F3) end-to-end

Lưới duyệt phim (CDK virtual scroll dạng list), lọc theo nguồn, tìm kiếm MiniSearch chuẩn hoá tiếng Việt. Phát hiện field `fileName` không tồn tại trong data model thật: [ADR-0008 addendum, 2026-08-25](./adr/0008-tim-kiem-client-side-minisearch.md#cập-nhật-sau-khi-accepted-2026-08-25-slice-browse-f3).

## 2026-08-25 — Index (F2) end-to-end

3 tầng catalog/delta/full-scan. Phát hiện mô hình tin cậy phải đổi từ nhị phân sang phân tầng do giới hạn quyền thật của Telegram API: [ADR-0010 addendum, 2026-08-25](./adr/0010-catalog-spec-v1-va-chien-luoc-indexing.md#cập-nhật-sau-khi-accepted-2026-08-25-slice-index-f2).

## 2026-08-24 — Sync & Hydration (F1.2/F1.3) end-to-end

Event log + snapshot, kênh state riêng tư, đồng bộ optimistic. Phát hiện Core Worker phải là singleton cấp tab: [ADR-0004 addendum, 2026-08-24](./adr/0004-mo-hinh-da-luong.md#cập-nhật-sau-khi-accepted-2026-08-24-slice-sync-f12f13).

## 2026-08-24 — Auth (F1.1) end-to-end

`TelegramGateway.login` thật + màn đăng nhập. Vá GramJS chạy sai trong Worker (`randomBytes` vỡ + kết nối nhầm IP thô): [ADR-0003 addendum, 2026-08-24](./adr/0003-chon-thu-vien-mtproto-gramjs.md#cập-nhật-sau-khi-accepted-2026-08-24-slice-auth-f11). SPIKE-01/02/03 đóng, mã nguồn xoá sau khi đóng (số liệu giữ ở [docs/spikes/README.md](./spikes/README.md)).

## 2026-08-24 — Dựng nền workspace

Scaffold workspace theo [ADR-0012](./adr/0012-trien-khai-static-pwa-va-cau-truc-workspace.md) (Nx-style monorepo, static PWA, `apps/web` không import `core-mtproto`/`core-download`).
