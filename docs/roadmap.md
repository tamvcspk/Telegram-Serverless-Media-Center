# Việc còn lại (Roadmap)

> **Cách dùng tài liệu này:** đây CHỈ là danh sách việc **sẽ làm** — gap còn thiếu so với 7 màn hình + hạ tầng. Việc đã xong → xem [docs/changelog.md](./changelog.md). Tại sao một giới hạn tồn tại (quyết định kiến trúc, đánh đổi đã chấp nhận) → xem ADR liên quan, không lặp lại rationale ở đây. Bài học kỹ thuật đã trả giá bằng debug thật → [docs/lessons.md](./lessons.md).
>
> **Nhãn trạng thái:** `[Chưa bắt đầu]` — chưa ai làm. `[Hoãn có chủ đích]` — đã quyết định hoãn, có lý do ghi rõ. `[Cần kiểm chứng thiết bị thật]` — code đã viết nhưng chưa có traffic/thiết bị thật xác nhận đúng.
>
> **Cách cập nhật:** khi bắt đầu làm một mục, xoá khỏi danh sách này và thêm mục mới vào [docs/changelog.md](./changelog.md) khi xong. Khi một slice mới phát hiện gap mới, thêm 1 dòng vào đúng nhóm bên dưới.

## UI theo từng màn hình

- **Browse (Màn hình 2):** filter Tất cả/Cá nhân/Cộng đồng — cần thêm field phân loại vào `SourceRef` ở tầng dữ liệu trước khi UI khớp được. `[Chưa bắt đầu]`
- **Browse → Collections:** tạo bộ sưu tập mới trực tiếp từ Browse (hiện phải qua tab BST trước). `[Chưa bắt đầu]`
- **Collections (Màn hình 3):** phân biệt `NO_ACCESS` (mất quyền truy cập, cho "Tham gia lại") vs `DELETED` (đã xoá file, cho "Gỡ khỏi BST") — cần bắt lỗi tầng MTProto. `[Hoãn có chủ đích]`
- **Sources (Màn hình 4):** `scanSource()` báo tiến trình theo số (vd "Đang nạp 1500/5000 tin") — cần job nền resumable, hiện chỉ có `MatProgressBar` indeterminate. `[Chưa bắt đầu]`
- **Sources:** `SyncStatus` (công cụ debug outbox) cần một chỗ đàng hoàng hơn, hiện tạm host cuối trang `home/sources`. `[Chưa bắt đầu]`
- **Ingest Editor (Màn hình 6):** lọc quyền ghi trước khi hiện icon ✏️ ở mỗi row Browse (hiện hiện ở MỌI row, kiểm tra quyền diễn ra ở đích đến). `[Hoãn có chủ đích]`
- **Ingest Editor:** retry/rollback khi `FLOOD_WAIT` rơi giữa chuỗi 3 RPC ghi liên tiếp của `publishCatalogDocument()` (`sendFile → pinMessage → deleteMessages`). Xem [ADR-0014 addendum, SPIKE-06](./adr/0014-mo-hinh-kenh-media-dung-chung-state-rieng-tu.md#cập-nhật-sau-khi-accepted-2026-08-28-spike-06). `[Chưa bắt đầu]`
- **Poster ảnh thật:** pipeline tải Telegram Photo (khác Document/video đã có) để thay placeholder gradient ở Browse/Collections — ngoài phạm vi F4, cần thiết kế riêng (tải ảnh lúc duyệt danh sách lớn dễ dính `FLOOD_WAIT`). `[Chưa bắt đầu]`
- **Player: UI chọn track khi có NHIỀU phụ đề cùng lúc (đa ngôn ngữ) chưa verify.** `player.ts` đã đọc/hiển thị `subs[]` — verify thật 2026-08-31 xác nhận ĐẠT cho phụ đề đơn ngôn ngữ (RPC `getSubtitleDocument()` + convert SRT→VTT + `<track>` gắn qua menu CC gốc của `<video controls>`, không tự vẽ UI riêng — xem [docs/changelog.md § 2026-08-31](./changelog.md#2026-08-31--player-verify-thật-phụ-đề-đơn-ngôn-ngữ--đạt)). Case nhiều `subs[]` cùng lúc trên một item (chọn đúng track, không lẫn nội dung) chưa test tay — hoãn có chủ đích, gộp chung vào lúc làm UI chọn track riêng thay vì test rời bây giờ với menu CC mặc định của trình duyệt. `[Hoãn có chủ đích]`

## Ingest

- **`tsmc-ingest` CLI (ADR-0013 mục 1) — verify Hạng C thật (2026-08-30) + subtitle upload đã nối dây (2026-08-30, chưa verify thiết bị thật).** `apps/tsmc-ingest` + `libs/core-ingest` (phân hạng A/B/C/D, kế thừa metadata theo series, gộp catalog) + `libs/core-mtproto/src/gateway-ingest.ts` (`uploadVideoDocument()`, `uploadSubtitleDocument()`). Pipeline: probe (`ffprobe` cục bộ) → phân hạng → remux/re-encode nếu cần → rút + upload phụ đề text → thumbnail → metadata (kế thừa từ tập trước cùng series) → upload → gộp + publish `catalog.json` một lần/batch. Chi tiết: [ADR-0013 § Cập nhật 2026-08-30, nối dây subtitle upload](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-08-30-nối-dây-subtitle-upload--phụ-đề-text-không-còn-bị-bỏ-phí). Còn thiếu (checklist ở [docs/pending-device-tests.md](./pending-device-tests.md)): verify subtitle upload bằng tài khoản thật; đối chiếu ngưỡng phân hạng A/B/C/D với file mẫu thật — **hoãn có chủ đích** (brainstorm 2026-08-30: cả ba hạng chỉ là biến thể của cùng cơ chế remux đã verify ở Hạng C). Hướng core lib Rust bọc FFmpeg (native FFI qua `ffmpeg-next`, cho một Tauri GUI tương lai): [SPIKE-09](./spikes/README.md#spike-09) đã chạy — 🟢 khả thi về tốc độ/đóng gói nhưng phát hiện rủi ro segfault tiến trình khi sai tham số (khác shell-out CLI hiện tại, vốn chỉ lỗi ở tiến trình con); điều kiện áp dụng nếu chọn hướng này ghi ở [ADR-0013 § Cập nhật 2026-09-03](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-03-spike-09--native-rust-ffi-cho-core-lib-tauri-tương-lai). `@tsmc_bot` (mục 2) và quyết định GUI Tauri vẫn để ngỏ, ngoài phạm vi CLI này. `[Cần kiểm chứng thiết bị thật]`

- **GUI ingest desktop (Tauri) — màn Đăng nhập + Chọn kênh đã code, đăng nhập + khôi phục session đã verify thật (2026-09-10); Chọn kênh chưa verify (2026-09-11); workspace ba vùng (A.3) đã có BẢNG METADATA (sửa file trước khi upload) TÁCH BIỆT khỏi HÀNG ĐỢI (sidebar trái, chỉ chứa file đã bấm Upload) + nút "Upload" cho CẢ 4 hạng A/B/C/D — Hạng C VÀ Hạng D đều đã verify thật qua tài khoản thật (2026-09-12) — chỉ còn thiếu THANH CHI PHÍ.** `apps/tsmc-ingest-desktop` (Cargo workspace: `ingest-rpc-trait`, `ingest-grammers`, `src-tauri`, cộng `ui/` — app Angular riêng, `@tsmc/tsmc-ingest-desktop-ui` trong pnpm workspace chung) — ported từ `tools/spike-10/rpc-trait` + `r3-grammers` sau khi [ADR-0017](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md) chọn `grammers-client` 0.10.0. 12 Tauri command đã wire — 4 cái bootstrap phiên đăng nhập (`check_session`/`request_login_code`/`submit_otp`/`submit_password`) + `load_saved_credentials`/`save_credentials` (nhớ credential ở app-data, KHÔNG phải `localStorage`) + 5/9 method `IngestRpc` (`resolve_channel`/`list_own_channels`/`create_channel`/`check_write_permission`/`read_pinned_catalog`, nối UI xong ở màn Chọn kênh, cộng `select_channel` không thuộc trait); `upload_video`/`upload_subtitle`/`publish_catalog`/`download_document` đã có implementation nhưng chưa wire — để dành slice workspace ba vùng (mockup A.3). Màn Đăng nhập khớp mockup A.4 (`MatStepper` 3 bước, cảnh báo ADR-0011 §5, đếm ngược `FLOOD_WAIT`), đăng nhập xong tự điều hướng sang route `/channel`. Hai bug thật phát hiện + vá cùng ngày (2026-09-11): (1) nhớ credential ở `localStorage` (bản đầu) tách theo ORIGIN phục vụ webview — `cargo tauri dev` và bản release không chia sẻ — sửa bằng `credentials.json` ở app-data (không phụ thuộc origin); (2) mockup gốc đòi "danh sách kênh ghi được" nhưng bảy thao tác gốc chỉ resolve được MỘT ref — thêm `list_own_channels()`/`create_channel()` vào trait (2 thao tác KHÔNG có tương ứng 1-1 phía TS, ngoại lệ có chủ đích, xem [ADR-0017 § Cập nhật 2026-09-11](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-11-ingestrpc-thêm-2-thao-tác-không-có-tương-ứng-1-1-phía-ts)) — màn Chọn kênh giờ có 3 đường: chọn từ danh sách kênh của admin, nhập ref trực tiếp, hoặc tạo kênh mới. Cả hai bug chi tiết ở [docs/changelog.md § 2026-09-11](./changelog.md#2026-09-11--gui-ingest-desktop-màn-chọn-kênh-thêm-picker--tạo-kênh-mới). Chặn ID thô tại form (bất biến #10) và chặn chọn kênh không phải `is_own` (bất biến #5) vẫn giữ nguyên cho nhánh nhập ref; hai nhánh còn lại theo cấu trúc luôn `is_own === true`. Verify thật 2026-09-10 (Đăng nhập): đăng nhập lần đầu (tài khoản KHÔNG 2FA) chạy đúng thiết kế hết Bước 1 → Bước 2 → "Đã đăng nhập"; đóng/mở lại app, `check_session` lần hai nhận diện đúng session cũ (kể cả sau migrate DC lúc login). `cargo build`/`cargo clippy`/`ng build` sạch (2026-09-11) nhưng CHƯA chạy qua `cargo tauri dev` + tài khoản thật cho picker/tạo kênh — checklist ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--màn-chọn-kênh-2026-09-11). Còn thiếu: nhánh 2FA (Bước 3) và mã OTP sai chưa test; verify Chọn kênh (cả 3 đường) bằng tài khoản thật; workspace ba vùng + Trình quản lý catalog/Nhật ký (2 màn A.4 còn lại); wire 4 method `IngestRpc` còn lại; số phận `tsmc-ingest` CLI hiện tại, tra metadata online (TMDB, opt-in), và `@tsmc_bot` vẫn để ngỏ, không chặn tiến độ. Chi tiết khung sườn gốc: [ADR-0017 § Cập nhật 2026-09-10](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-10-khung-sườn-appstsmc-ingest-desktop--code-thật-lần-đầu).
  **Cập nhật 2026-09-12 (workspace ba vùng, A.3 — chỉ vùng HÀNG ĐỢI):** crate Rust thứ tư `ingest-ffmpeg` (probe bằng `ffmpeg-next` native, verify đối chiếu 1-1 với `ffprobe` CLI thật) + 2 command mới `list_media_files`/`probe_media` (không đụng MTProto/`AppState`) + route `/workspace` (kéo thả file/folder qua sự kiện gốc webview, hiện hạng màu A/B/C/D ngay bằng `classifyCompatRank()` của `@tsmc/core-ingest` — lần đầu app này import `lib-core` thật). Cố ý CHƯA làm ở slice này (theo lựa chọn phạm vi của user): bảng metadata điền xuống hàng loạt, thanh chi phí, wire 4 method `IngestRpc` còn lại (`upload_video`/`upload_subtitle`/`publish_catalog`/`download_document`). Đóng gói 7 DLL FFmpeg cho bản phân phối thật (addendum ADR-0013 2026-09-04) cũng chưa làm — máy dev hiện tại chạy được nhờ có sẵn `vcpkg install ffmpeg:x64-windows`, không phải bằng chứng chạy được trên máy sạch. **Verify thật 2026-09-12 (cùng ngày):** user tự chạy `cargo tauri dev`, kéo 5 file thật từ một season vào vùng HÀNG ĐỢI — cả 5 ra đúng hạng màu kỳ vọng (3 `.mkv` → C, 1 `.avi` → D, 1 `.mp4` cắt đoạn → B), xem [docs/changelog.md § 2026-09-12, verify thật](./changelog.md#2026-09-12--gui-ingest-desktop-workspace-ba-vùng--verify-thật-vùng-hàng-đợi-bằng-file-season-thật).
  **Cập nhật tiếp trong cùng slice (BẢNG METADATA):** thêm vùng BẢNG METADATA của A.3 — bảy cột (checkbox/Tên file/Title/Season/Ep/Năm/hạng) hiện dưới dạng `cdk-virtual-scroll-viewport` (không phải `mat-table`, giữ nhất quán pattern div+CDK đã dùng ở vùng hàng đợi/danh sách kênh), sửa trực tiếp từng ô (commit lúc blur, không phải mỗi phím gõ), 3 thao tác hàng loạt trên các dòng đã chọn: "Điền xuống" (Title/Season/Năm, lấy giá trị dòng ĐẦU TIÊN đã chọn), "Đánh số tập tự động" (Season dòng đầu áp cho tất cả, Episode tăng dần), "Xoá khỏi hàng". Seed/kế thừa metadata dùng lại NGUYÊN VẸN `seedMetadataFromFilename()`/`inheritMetadata()` (`@tsmc/core-ingest`) theo đúng thứ tự tuần tự mà `apps/tsmc-ingest/src/commands/upload.ts::resolveMetadataForFile()` đã verify — khác CLI ở chỗ không có confirm dialog chặn luồng, kết quả kế thừa chỉ là giá trị gợi ý sẵn trong ô (đúng nguyên tắc UX A.2 mục 3), admin tự sửa đè dòng kế thừa sai (thường lúc đổi series/gặp trailer). Hạng D tự động bỏ chọn checkbox (mockup A.5). `ng build`/`ng serve`/`npm run lint` sạch — CHƯA verify bằng `cargo tauri dev` thật.
  **Cập nhật tiếp trong cùng slice ("Bắt đầu upload", chỉ Hạng A/B/C):** đóng nốt 4 method `IngestRpc` còn lại thành Tauri command — `upload_video` (bắn sự kiện `"upload-progress"`, nhận `CancelFlag` cho `cancel_upload()`), `upload_subtitle`, `publish_catalog` (`download_document` để dành màn "Trình quản lý catalog"). Thêm 3 hàm `ingest-ffmpeg` (`remux`/`extract_thumbnail`/`extract_subtitles`, port từ `tools/spike-09` đã verify thật) gộp qua command `prepare_upload` (remux+thumbnail+rút phụ đề, bắn sự kiện `"pipeline-stage"`) — verify trực tiếp bằng chạy tay cả pipeline trên fixture SPIKE-09 trước khi wire Tauri. Phụ đề upload ĐẦY ĐỦ như CLI: nhúng (Hạng C) lẫn rời cạnh file (`list_dir_entries` + `matchSidecarSubtitles()` phía TS). `startUpload()` (Angular) xử lý tuần tự từng item, `withFloodWaitRetry()` bọc mọi RPC MTProto thật (đếm ngược đúng số giây, tự thử lại — không né FLOOD_WAIT), publish catalog ĐÚNG MỘT LẦN cho cả batch sau cùng. Hạng D vẫn bị chặn có chủ đích ở LẦN commit đầu (cần nhánh re-encode video, mở khoá ngay trong cùng slice — xem addendum tiếp theo). `cargo build`/`cargo clippy` (0 warning) + `ng build`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch — CHƯA verify upload thật bằng tài khoản Telegram (RPC MTProto thật đầu tiên của app này ngoài đăng nhập/chọn kênh). Chi tiết: [docs/changelog.md § 2026-09-12, wire Bắt đầu upload](./changelog.md#2026-09-12--gui-ingest-desktop-workspace-ba-vùng--wire-bắt-đầu-upload-hạng-abc).
  **Cập nhật tiếp trong cùng slice (verify Hạng C thành công + mở khoá Hạng D):** user tự chạy `cargo tauri dev` + tài khoản thật, upload MỘT file Hạng C THÀNH CÔNG — xác nhận đường ống chạy đúng trên tài khoản thật (chi tiết từng bước chưa xác nhận riêng, xem checklist). Theo yêu cầu tiếp: nút hành động đổi tên "Upload (N)", dời lên `.metadata-toolbar`, appearance `mat-raised-button`. Mở khoá Hạng D: thêm `reencode_to_mp4()` vào `ingest-ffmpeg` (decode+encode THẬT cả video sang H.264/AAC, port từ ví dụ CHÍNH THỨC `examples/transcode-x264.rs` của `ffmpeg-next` — không tự chế pattern pts/dts mới cho phần rủi ro nhất) — verify trực tiếp bằng fixture AVI/Xvid tổng hợp (không crash, `ffprobe -count_frames` + `ffmpeg -f null -` xác nhận decode lại sạch) trước khi wire. `RemuxModeDto` (`copy`/`reencode_audio`/`reencode_all`) thay cho cờ bool cũ; `startUpload()` hỏi xác nhận `window.confirm()` RIÊNG TỪNG FILE Hạng D (mockup A.2 mục 1, kèm số phút ước tính) trước khi gọi — từ chối thì bỏ qua đúng file đó, batch chạy tiếp. `cargo build`/`cargo clippy` (0 warning) + `ng build`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch — CHƯA verify Hạng D bằng tài khoản Telegram thật (chỉ verify pipeline ffmpeg cục bộ). Chi tiết: [docs/changelog.md § 2026-09-12, verify Hạng C + mở khoá Hạng D](./changelog.md#2026-09-12--gui-ingest-desktop-verify-thật-hạng-c--mở-khoá-hạng-d-re-encode-video).
  **Cập nhật tiếp trong cùng slice (verify Hạng D thành công + redesign hai danh sách + shared dialog):** user xác nhận "Hạng D re-encode và upload thành công" qua tài khoản thật. Theo yêu cầu tiếp, redesign lớn: BẢNG METADATA (cột phải) và HÀNG ĐỢI (sidebar trái) TÁCH BIỆT hẳn (trước đó lặp lại cùng một danh sách) — bảng metadata chỉ chứa file CHƯA upload (sửa được), hàng đợi RỖNG cho tới khi bấm "Upload", lúc đó dòng đã chọn bị xoá khỏi bảng và đẩy sang hàng đợi (kèm `mat-progress-bar` mỏng dưới tên file — xác định khi có % byte thật ở bước upload video, không xác định ở các bước cục bộ khác), ở lại đó tới khi dismiss tay. Thêm global progress kiểu Apple Mail ("Đã tải X/N file — P%") dưới hàng đợi. Thêm shared `DialogService`/`ConfirmDialog` (Angular Material thật, `shared/dialog/`) thay `window.confirm()` — áp dụng vào xác nhận re-encode Hạng D, nút xác nhận tô màu warn đúng mockup A.2 mục 1. `ng build`/`ng serve`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch (round này thuần Angular, Rust không đổi) — CHƯA verify redesign bằng tài khoản thật. Chi tiết: [docs/changelog.md § 2026-09-12, verify Hạng D + tách hàng đợi](./changelog.md#2026-09-12--gui-ingest-desktop-verify-thật-hạng-d--tách-hàng-đợi-khỏi-bảng-metadata--shared-dialog).
  **Cập nhật tiếp (hàng đợi đổi sang `mat-list`/`mat-progress-spinner` + vá icon-button):** theo yêu cầu user — mỗi dòng hàng đợi đổi từ div tự vẽ sang `mat-list-item` thật (`matListItemAvatar` = icon trạng thái hoặc `mat-progress-spinner`, `matListItemTitle` = tên file ellipsis + tooltip đầy đủ, `matListItemLine` = tên stage, `matListItemMeta` = nút huỷ/dismiss), `mat-progress-bar` (thanh ngang) đổi sang `mat-progress-spinner` (vòng tròn) với số %/giây FLOOD_WAIT hiện GIỮA vòng. Vá riêng bug lệch ripple/icon của icon-button nhỏ (ép cứng `width`/`height` xung đột với padding nội bộ Material tính từ `calc((state-layer-size - icon-size)/2)`) — sửa bằng 2 custom property thay vì width/height. Chi tiết: [docs/changelog.md § 2026-09-12, mat-list/spinner](./changelog.md#2026-09-12--gui-ingest-desktop-hàng-đợi-đổi-sang-mat-listmat-progress-spinner--vá-lệch-icon-button).
  **Cập nhật tiếp (vá 4 bug thật từ phiên test kế tiếp):** (1) spinner đứng yên — nguyên nhân gốc `provideNoopAnimations()` (Material tự tắt hẳn animation của spinner khi phát hiện noop, không chỉ tắt transition trang trí) → đổi `provideAnimationsAsync()`; (2) sidebar cuộn ngang → `overflow-x: hidden` trên viewport; (3) upload không thấy spinner lúc mới vào stage (value=0% = vòng trống theo đúng thiết kế MDC) → giữ "indeterminate" tới khi có mẫu tiến trình thật; (4) **bug thật nghiêm trọng nhất:** `reencode_to_mp4()` lỗi "invalid argument" trên file thật — đúng rủi ro đã ghi nhận trước (pixel format lạ/kích thước lẻ không qua `scale`/`format` filter ngầm như CLI shell-out) — vá bằng `scaling::Context` bắt buộc ép YUV420P + kích thước chẵn, verify lại bằng fixture TÁI HIỆN ĐƯỢC đúng lỗi gốc (641×361, `yuvj444p`) chạy sạch, không hồi quy fixture cũ. `cargo build`/`cargo clippy` (0 warning) + `ng build`/`ng serve`/`npm run lint`/`npm run test:libs`/`npm run docs:check` sạch — CHƯA verify cả 4 fix bằng tài khoản thật (chỉ #4 verify bằng fixture tái hiện lỗi). Chi tiết: [docs/changelog.md § 2026-09-12, vá 4 bug thật](./changelog.md#2026-09-12--gui-ingest-desktop-vá-4-bug-thật-từ-phiên-test-tiếp-theo-spinner-đứng-yên-cuộn-ngang-upload-không-thấy-spinner-re-encode-lỗi-invalid-argument). `[Cần kiểm chứng thiết bị thật]`
  **Wrap phiên (2026-09-12) — open issue phát hiện:** progress upload luôn `indeterminate` với Hạng B/C/D do lệch correlation id `path` giữa backend/frontend — chi tiết nguyên nhân ở [docs/changelog.md § 2026-09-12, wrap phiên](./changelog.md#2026-09-12--gui-ingest-desktop-wrap-phiên--phát-hiện-chưa-vá-open-issue-progress-upload-luôn-indeterminate).
  **Cập nhật 2026-09-13 (vá bug trên, [ADR-0018](./adr/0018-task-id-lam-khoa-tuong-quan-ipc-ingest-desktop.md)):** Task ID (UUID sinh phía Angular) thay `path` làm khoá tương quan cho `upload_video`/`prepare_upload`/`cancel_upload` và hai event `"upload-progress"`/`"pipeline-stage"`; `cancel_upload` giờ idempotent theo id (vá luôn một race huỷ-nhầm-file-kế-tiếp có thật ở bản cũ). `cargo build`/`cargo clippy`/`ng build` sạch — **verify 2026-09-13, ĐẠT** (user xác nhận qua tài khoản thật). Chi tiết: [docs/changelog.md § 2026-09-13, PR1](./changelog.md#2026-09-13--gui-ingest-desktop-vá-bug-progress-indeterminate--task-id-uuid-thay-path-làm-khoá-tương-quan-ipc-adr-0018).
  **Cập nhật tiếp (PR2, cùng ngày):** hydration khi remount (`get_current_task()`/`clear_current_task()`), `DraftStore`/`QueueStore` root-provided (state sống xuyên route, batch async/await tiếp tục chạy ngầm khi rời `/workspace`), `canDeactivateWorkspace` + `onCloseRequested` cảnh báo mất Draft/tác vụ đang chạy trước khi rời màn/đóng app, dialog Hạng D đổi sang MỘT dialog tổng hợp có toggle từng file (thay hỏi từng file riêng). `cargo build`/`cargo clippy`/`ng build` sạch (321.72KB, dưới budget 500KB sau khi vá 1 bug ngân sách bundle thật phát hiện lúc build) — CHƯA verify bằng tài khoản Telegram thật. Chi tiết: [docs/changelog.md § 2026-09-13, PR2](./changelog.md#2026-09-13--gui-ingest-desktop-hydration--singleton-store--guard-rời-màn--dialog-hạng-d-gộp-pr2-adr-0018).
  **Cập nhật tiếp (chặn kích thước file, cùng ngày):** đóng gap thật của SPIKE-10 M7 — `MAX_UPLOAD_BYTES = 4_000_000_000` chặn TRƯỚC khi mở kết nối upload (`ingest-grammers`), trả `FileTooLarge` thay vì để Telegram từ chối bằng `FILE_PARTS_INVALID` thô. Phát hiện qua verify thật (thả `sample-4gb.mp4`, 5 file khác dưới trần OK). CHƯA verify lại sau vá. Chi tiết: [docs/changelog.md § 2026-09-13](./changelog.md#2026-09-13--gui-ingest-desktop-chặn-kích-thước-file-trước-khi-upload--đóng-gap-thật-của-spike-10-m7-adr-0017).
  **Cập nhật tiếp (vá bug KHÔNG ĐÓNG ĐƯỢC APP, cùng ngày):** regression từ PR2 — thiếu quyền `core:window:allow-destroy` (thư viện Tauri tự gọi `destroy()` ở MỌI lần đóng cửa sổ nếu có đăng ký `onCloseRequested`, không chỉ nhánh có cảnh báo). Vá bằng thêm quyền + đổi `close()` → `destroy()`. **Verify 2026-09-13, ĐẠT.** Chi tiết: [docs/changelog.md § 2026-09-13](./changelog.md#2026-09-13--gui-ingest-desktop-vá-bug-không-đóng-được-app--thiếu-quyền-corewindowallow-destroy-regression-từ-pr2).
  **Cập nhật tiếp (đóng gói DLL FFmpeg + trả lời Đ2, cùng ngày):** `cargo tauri build` chạy trọn lần đầu (MSI + NSIS) nhờ `build.rs` mới copy 7 DLL FFmpeg cạnh `.exe`. Nhân tiện đo Đ2 (SPIKE-10, để ngỏ từ 2026-09-07): encoder H264 thật ở Hạng D là `h264_mf` (Windows), không phải `libx264` — **giữ LGPL, không kéo GPL**. `cargo build`/`cargo clippy` (0 warning, gồm example `check_h264_encoder` mới) sạch. **Verify Đ1 2026-09-13, ĐẠT** — user xác nhận cài+chạy bản đóng gói trên máy sạch (không vcpkg/LLVM/Rust/Node) thành công, chưa có số liệu chi tiết (MB installer, danh sách file). Chi tiết: [docs/changelog.md § 2026-09-13](./changelog.md#2026-09-13--gui-ingest-desktop-đóng-gói-dll-ffmpeg-vào-bản-phân-phối--trả-lời-đ2-spike-10-giữ-được-lgpl), [ADR-0013 § addendum](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-13-trả-lời-đ2--đóng-gói-dll-ffmpeg-thật-lần-đầu). Đ3 (SPIKE-10, chi phí viết mới) chưa đo — không chặn tiến độ.
  **Cập nhật tiếp (PR3 — tích hợp TMDB, cùng ngày, [ADR-0019](./adr/0019-tich-hop-tra-cuu-tmdb-o-buoc-draft.md)):** nút "Tra TMDB" mỗi dòng bảng metadata — command Rust mới `tmdb_search`/`tmdb_has_key`/`tmdb_save_key` (không thuộc `IngestRpc`, dùng `reqwest`), key lưu app-data cùng kiểu `credentials.json`. Chưa có key → dialog nhập key trước (opt-in tự nhiên). Có key → dialog tìm kiếm, admin tự chọn kết quả (poster/tên/năm), chỉ điền Title/Năm khi xác nhận — không tự động. `cargo build`/`cargo clippy`/`ng build` sạch. **Verify 2026-09-13, ĐẠT** (gồm cả nhánh `kind: 'episode'` và đóng dialog không chọn gì) — user xác nhận bằng API key TMDB thật, field response khớp giả định trong ADR. Chi tiết: [docs/changelog.md § 2026-09-13, PR3](./changelog.md#2026-09-13--gui-ingest-desktop-tích-hợp-tra-cứu-tmdb-ở-bước-draft-pr3-adr-0019).
  **Cập nhật tiếp (khai tử `tsmc-ingest` CLI + `@tsmc_bot`, dọn spike, cùng ngày, [ADR-0013 § addendum](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md#cập-nhật-sau-khi-accepted-2026-09-13-khai-tử-tsmc-ingest-cli--tsmc_bot--chốt-ba-thành-phần-gốc-còn-đúng-một)):** GUI ingest desktop đã đạt parity đầy đủ (cả 4 hạng, đóng gói được, TMDB) — không còn lý do giữ CLI song song. Đã xoá `apps/tsmc-ingest/` (không ai import package này, xác nhận trước khi xoá) + `tools/spike-09/`/`tools/spike-10/` (nguồn `ingest-ffmpeg`/`ingest-grammers`, đã port xong từ lâu) khỏi repo — lịch sử đầy đủ còn trong git log, số liệu đo được vẫn giữ nguyên trong `docs/spikes/README.md`. `@tsmc_bot` **chưa từng có dòng code nào** (chỉ là kế hoạch trong ADR-0013 gốc) — deprecated vì GUI đã tự làm được `/publish`/`/check`, không có gì để xoá. Dọn theo: script `build:ingest` (`package.json`), boundary `app-ingest` (`eslint.config.mjs`), sơ đồ `docs/architecture.md`/README gốc.

  **Việc còn thiếu, tổng hợp lại (2026-09-13) — chưa làm/chưa verify, KHÔNG chặn tiến độ trừ khi ghi rõ:**
  - Thanh chi phí "TRƯỚC KHI CHẠY" (ước tính byte/phút trước khi upload) — chưa làm từ đầu.
  - 2 màn A.4 còn lại: Trình quản lý catalog, Nhật ký — chưa bắt đầu.
  - `download_document` (method thứ 9 `IngestRpc`) — có implementation, chưa wire UI.
  - Crash-recovery (đóng hẳn app giữa chừng upload) — cố ý để ngỏ (ADR-0018).
  - **Ngưỡng upload theo tài khoản (Premium 4GB / thường 2GB) — đã vá 2026-09-14, verify ĐẠT nhánh Premium, hoãn nhánh thường (không có tài khoản để test).** `MAX_UPLOAD_BYTES_PREMIUM`/`MAX_UPLOAD_BYTES_FREE` thay hằng số chung cũ, `GrammersIngestRpc::new()` đọc cờ Premium thật qua `get_me()` lúc đăng nhập xong — xem [ADR-0017 § addendum 2026-09-14](./adr/0017-grammers-cho-cong-cu-ingest-desktop.md#cập-nhật-sau-khi-accepted-2026-09-14-vá-tier-aware-upload-threshold), [docs/changelog.md § 2026-09-14](./changelog.md#2026-09-14--gui-ingest-desktop-verify-tier-aware-upload-threshold--đạt-cho-premium-hoãn-nhánh-thường). `cargo build`/`cargo clippy` sạch. Nhánh KHÔNG Premium để mở tới khi có tài khoản phù hợp — checklist ở [docs/pending-device-tests.md](./pending-device-tests.md#gui-ingest-desktop-appstsmc-ingest-desktop--tier-aware-upload-threshold-2026-09-14).
  - TMDB nâng cao: genres/cast/director/poster (cần thêm endpoint `credits` + upload poster thành message riêng) — để dành PR sau.
  - **Mã hoá thông tin lưu ở app-data — KHÔNG lưu plaintext** (`credentials.json`, `tmdb_api_key.json` hiện đều plaintext, chấp nhận tạm thời theo ADR-0011 vì "không phải secret server") — ý định gốc ở SPIKE-10 § Tech stack đã ghi "R3/R4: file mã hoá hoặc keychain OS qua Tauri" nhưng lúc code đã đơn giản hoá thành plaintext, chưa quay lại làm đúng ý định ban đầu.
  - **Màn Settings** — chưa có; TMDB opt-in hiện phải "mượn" luồng first-use dialog để bù (ADR-0019 mục 2) vì không có nơi tập trung quản lý key/mã hoá/tuỳ chọn khác. Một màn Settings thật sẽ cần cho cả TMDB key lẫn cơ chế mã hoá app-data ở trên.
  - Nhánh 2FA/OTP sai lúc đăng nhập — chưa test.
  - Verify Chọn kênh cả 3 đường (danh sách/nhập ref/tạo mới) bằng tài khoản thật — chưa xác nhận đầy đủ.
  - Đ1 (SPIKE-10): số liệu chi tiết (MB installer, danh sách file) — chỉ tham khảo, không chặn.
  - Đ3 (SPIKE-10, chi phí viết mới) — chưa đo, chỉ tham khảo.

## Index / quét nguồn

- **Hashtag/Forum topic làm tín hiệu index (đã code 2026-08-29):** hashtag làm tín hiệu suy luận metadata + Forum Topic làm category — cả hai đã code, xem [docs/changelog.md](./changelog.md#2026-08-29--index-category-theo-forum-topic--hashtag-làm-tín-hiệu-fallback). Còn thiếu: verify bằng kênh Forum + hashtag thật trên thiết bị thật (unit test dùng fake gateway) — checklist chi tiết ở [docs/pending-device-tests.md](./pending-device-tests.md#index-forum-topic-category--hashtag-fallback-2026-08-29). `[Cần kiểm chứng thiết bị thật]`

## Sync & dữ liệu

- **Kênh state riêng:** `publishSnapshot()` chưa được xác nhận trên thiết bị thật riêng (khác kênh test dùng cho SPIKE-06). Xem [ADR-0009 addendum, 2026-08-24](./adr/0009-dong-bo-state-event-log-va-snapshot.md#cập-nhật-sau-khi-accepted-2026-08-24-slice-sync-f12f13). `[Cần kiểm chứng thiết bị thật]`
- **Kênh state riêng:** nén snapshot (>200 event hoặc snapshot >7 ngày) và gộp nhiều kênh state — xem [ADR-0014](./adr/0014-mo-hinh-kenh-media-dung-chung-state-rieng-tu.md) — chưa tự phát sinh trong một lần dùng bình thường nên chưa qua kiểm chứng thiết bị thật. `[Cần kiểm chứng thiết bị thật]`
- **Kênh state riêng — nén định kỳ có thể cấu hình + vá trần phân trang khi hydrate:** brainstorm 2026-08-29 (chưa viết ADR addendum, chỉ ghi nhận ở roadmap). Một ràng buộc kiến trúc (mục 1) + ba việc nên làm cùng lúc (mục 2-4):
  1. **KHÔNG dùng bot** cho việc này — kênh state là 1-đổi-1 riêng tư tuyệt đối, cấm thêm bất kỳ thành viên nào kể cả bot (CLAUDE.md bất biến #5, [ADR-0014](./adr/0014-mo-hinh-kenh-media-dung-chung-state-rieng-tu.md)) — khác hẳn kênh media, nơi `@tsmc_bot` ([ADR-0013](./adr/0013-bot-dong-hanh-va-pipeline-ingest.md)) làm admin được vì kênh đó vốn multi-user. Cũng không thể chạy như một cron server-side (ADR-0001: không thành phần server nào trong đường chạy, kể cả bảo trì) — cách duy nhất khả thi trong kiến trúc này là compaction cơ hội (opportunistic) ngay trong app, chạy bởi chính session MTProto của user, mỗi khi tab leader hoạt động — đúng cơ chế `maybeCompact()` đã có, không có cách "chạy hộ" nào khác.
  2. **Đưa ngưỡng `COMPACTION_MAX_SNAPSHOT_AGE_MS`** (`libs/core-sync/src/compaction.ts`, hiện hardcode 7 ngày) **thành cấu hình được ở Settings** (ví dụ mặc định 7 ngày như ADR-0009 gốc, cho phép chỉnh, ví dụ 30 ngày) — đổi **giá trị mặc định** khỏi 7 ngày là sửa tham số của Quyết định đã Accepted ở ADR-0009, cần addendum khi thực sự code, không chỉ sửa số; còn lại (thêm khả năng cấu hình, giữ mặc định 7 ngày) không đụng nội dung Quyết định gốc. Ngưỡng >200 event vẫn giữ làm lưới an toàn cứng — không đổi, không cấu hình được, đảm bảo dùng nặng vẫn nén dù chưa tới hạn ngày.
  3. **Phát hiện một trần ẩn khi hydrate — quan trọng hơn cả việc cấu hình:** `fetchEventsSince()` (`libs/core-mtproto/src/gateway-sync.ts:128`) gọi `getMessages({..., limit: FETCH_EVENTS_PAGE_LIMIT })` với `FETCH_EVENTS_PAGE_LIMIT = 500`, **một trang duy nhất, không có vòng lặp phân trang**. Compaction bình thường giữ event luôn dưới ~200 nên 500 dư dả — nhưng nếu compaction KHÔNG chạy đủ lâu (đúng kịch bản "không dọn định kỳ" mà mục này lo ngại) và số event vượt 500, một lần **hydrate mới** (thiết bị mới, hoặc app dọn IndexedDB) sẽ **âm thầm chỉ replay 500 event đầu, bỏ sót phần còn lại** — đây mới là cách thật state có thể "mất" (không phải Telegram xoá gì — event vẫn còn nguyên trên kênh, chỉ là code không đọc hết). Cần: hoặc `fetchEventsSince()` tự lặp gọi `getMessages` tới khi hết, hoặc `hydrate()` phát hiện đã chạm đúng `FETCH_EVENTS_PAGE_LIMIT` (dấu hiệu "có thể còn dữ liệu") và gọi tiếp — không được coi một trang là đủ.
  4. Thêm chỉ báo ở Settings: "state chưa nén trong N ngày" (đã gợi ý sẵn ở [ADR-0009 Hệ quả](./adr/0009-dong-bo-state-event-log-va-snapshot.md#hệ-quả) — "thêm chỉ báo tình trạng đồng bộ trong Cài đặt", chưa làm) — khác `SyncStatus` debug tool ở mục Sources bên trên (đó là công cụ dev, đây là tín hiệu cho user thường). Cùng chỗ đặt UI cấu hình ngưỡng ở mục 2.

  `[Chưa bắt đầu]`

## Download / hardening

- **CDN_REDIRECT:** code đã viết theo đặc tả TL (AES-CTR qua WebCrypto + verify SHA-256, `libs/core-mtproto/src/gateway-download.ts`) nhưng chưa từng gặp traffic thật (SPIKE-02 vẫn 0/250 lần). Không có cách chủ động kích hoạt mà không tạo tải giả lên hạ tầng Telegram. Xem [docs/architecture.md §7](./architecture.md#7-rủi-ro-lớn-nhất--trạng-thái-kiểm-chứng). `[Cần kiểm chứng thiết bị thật]`
- **Trần song song 8:** rủi ro `FLOOD_WAIT` ở mức tải cao nhất mới có Settings UI (2026-08-28) — hiện chỉ có unit test với fake gateway, chưa có traffic thật kiểm chứng. `[Cần kiểm chứng thiết bị thật]`
- **Scheduler ưu tiên P0–P3** (ADR-0006 §2, "known gap") — cần tính năng prefetch đa video trước, app hiện chưa có tính năng đó nên chưa tới lượt làm. `[Chưa bắt đầu]`
